> **历史规格（V1，非当前设计事实来源）**
>
> 本文件记录 2026-07-22 至 2026-07-24 的麻布封面与单页自由拼贴方向。2026-07-30 已选定 `binder-archive（透明活页档案 + 周记网格主页）`。Phase 1 以 `baozi-space-prd.md` 为准；正式 V2 设计规范将在用户补齐页面内容并确认 PRD 后，于 Phase 2 产出。除明确复用的可访问性和素材原则外，不得直接按本文件实施 V2。

1. # baozi.space 个人网站规划与设计规格

   状态：正式开发已启动（Astro 生产骨架 + 首页手账 + 内容集合）；等待真实素材与文案  
   更新时间：2026-07-22

   ## 1. 项目定位

   `baozi.space` 是包子的个人网站、公开档案与数字手账。

   网站名称：

   > 中华一番包子铺

   它不是传统博客、餐饮品牌官网、作品集模板或 SaaS 仪表盘，而是一本文档尺度近乎无限延展的数字手账。用户不是在手账之外浏览网页，而是在手账内部阅读、翻页和操作内容。

   核心表达：

   > 打开一本属于包子的手账，在纸张、照片、标签和手写记录之间认识他的文章、想法、摄影、酒、书、音乐和 AI 创作。

   ## 2. 已确认的核心概念

   ### 2.1 网站就是手账本

   - 手账不是网页中的一个装饰容器，而是整个网站的空间与交互骨架。
   - 进入网站后，浏览器视口应被手账页面主导。
   - 页面向上下或后续跨页继续延展，形成“无限大的手账”感。
   - 不重点展示桌面、房间或周边环境，避免再次把手账缩小成场景中的一个物件。

   ### 2.2 启动体验

   访问流程：

   1. 访问者输入网址并回车。
   2. 页面出现一本合上的手账。
   3. 手账以轻量、自然的方式打开。
   4. 打开后直接进入首页页面。（2026-07-24 起首页为单页平铺，见 3.5）
   5. 用户在手账中阅读、移动部分物件并点击栏目入口。

   启动动效应有仪式感，但不能过长。桌面端优先完整表现开本过程；移动端可简化为封面打开、页层展开或纵向进入页面。

   ## 3. 当前视觉基准

   ### 3.1 纸张背景

   已确认的背景方向：

   - 主底色为白色。
   - 使用非常浅的灰色点阵、方格或细网格。
   - 网格密度参考日式 planner / PAL 手账纸。
   - 网格必须柔和，不应形成明显工程纸或 Excel 表格感。
   - 可加入极轻微的纸张纤维、印刷不均、压痕和折页阴影。
   - 不使用明显米黄、牛皮纸、复古旧纸、深色纸张作为主体背景。

   ### 3.2 页面尺度

   > 2026-07-24 更新：首页基线由「双页跨页」改为「单页平铺网点纸」，见 3.5；本条中双页/中缝相关描述已过时，仅作历史参考。

   - ~~桌面端首页采用打开的双页跨页。~~（已过时，见 3.5）
   - 手账应几乎铺满整个视口。
   - 可看到中缝、纸页边界和轻微圆角，但外部背景仅作为辅助。
   - 用户应感到自己正在浏览一个巨大手账，而不是俯视一本小本子。
   - 后续栏目可通过连续翻页、跨页切换或纵向延展继续浏览。

   ### 3.3 版式基准

   当前确认的版式不是规则网页栅格，而是参考真实日式手账的自由编排。

   必须具备：

   - 灵活、不完全对称的排版
   - 大小不同的内容块
   - 手写文字与正式文字混排
   - 小尺寸图片与局部重点物件
   - 印章、箭头、圈画、高亮、贴纸和批注
   - 大片留白与局部密集记录并存
   - 轻微错位、重叠和不同方向的纸片
   - 可辨认但不过度整齐的栏目入口

   必须避免：

   - 固定 `2 × 4` 或其他整齐入口矩阵
   - 仪表盘、卡片墙、应用启动器式布局
   - 八个物件大小完全一致
   - 所有栏目名称位于同一基线
   - 物件过大导致手账页面失去尺度
   - 为了“丰富”而平均填满全部空白

   ### 3.4 物件比例

   - 手账纸张和书写空间是视觉主角。
   - 物件总体尺寸应比上一版概念图更小。
   - 单个物件通常不应占据半页或成为独立产品展示。
   - 相机、啤酒、机器人等识别性物件可以作为局部焦点，但不能让页面看起来像商品陈列。
   - 照片和便利贴允许形成小组，但需要保留周围的书写和呼吸空间。

   ### 3.5 首页排版规范（Layout Guideline v1 · 2026-07-24 起生效）

   设计理念：**Accumulation over Composition**——物件是包子慢慢贴上去的，不是被排版出来的。首页是一张平铺的网点纸（单页），不是双页跨页，不是九宫格。

   **硬约束（可测量）：**

   - 布局：Organic Layout，禁止九宫格 / Bento Grid / 等宽卡片 / 对齐线。
   - 覆盖度：全部物件外接矩形合计 ≤ 60% 页面面积（即可验证的留白 ≥ 40%）。
   - 角度：物件 |θ| ≤ 5°；装饰件 ≤ 8°；禁止 > 8°。
   - 层级：主锚点（手账本 / Blog）视觉面积约 25–32%；其余入口 8–15%；装饰 2–5%。
   - 重叠：入口物件互不重叠；装饰可轻压入口边缘（遮挡 ≤ 15% 且不遮标签）；装饰间可重叠形成 3+ 层。
   - 间距：物件间距不得使用统一数值，像人随手放。
   - 反馈：仅当前悬停项轻微反馈，其余入口保持安静；禁止全体高亮 / 发光。
   - 可点性：入口可点区域 ≥ 44px（含移动端）。

   **入口与装饰：**

   - 入口名单显式维护，当前为四个：Blog（手账本 · A-）、Thoughts（便利贴 · B）、Photos（相机 · A）、About（包子 · A）。
   - 其余物件（唱片、贴纸、花、印章、手绘等）为纯装饰，不可点击；栏目内容充实后可手动晋升为入口。
   - 交互目标：贴纸类可拖动；印章 / 手绘固定；点击入口 → 浮现栏目名 → 再点进入。（现为过渡页自动进入，首页重建时对齐。）

   **阅读路径：** 单页 S 型视觉流，从主锚点出发，不设全局居中对齐。

   **移动端：** 设计坐标系 1200×800；≥ 660px 用 transform: scale 等比缩放整张纸（构图与相对位置不变）；< 660px 切换纵向重排（四入口垂直列表、≥ 44px、装饰精简或隐藏）。

   **素材：** 全部首页物件遵循 `baozi-space-assets.md`；真实素材未到位前不摆空壳占位（沿用空状态规则）。

   ## 4. 视觉材料系统

   ### 4.1 文字

   页面文字分为四类：

   1. **正文**：用于文章摘要、说明和较长内容，保持清晰可读。
   2. **手写文字**：用于欢迎语、日常记录、旁注和情绪表达。
   3. **标签文字**：用于日期、栏目、状态、分类和小纸签。
   4. **装饰性文字**：用于圈画、强调、英文短语、拟声词和手写标题。

   字体不能全部使用同一种手写体。应以可读正文为基础，局部加入自然手写和印刷字感。

   ### 4.2 白色描边物件

   白色描边物件的定义：

   - 来源为真实照片或真实物件图。
   - 主体经过抠图处理。
   - 外圈保留明显但不过厚的白色描边。
   - 加入轻微阴影，使其像贴在手账中的照片贴纸。
   - 边缘可略有手工裁切感，但不能过度卡通化。

   ### 4.3 照片

   可使用：

   - 白色描边抠图照片
   - 拍立得照片
   - 普通矩形照片
   - 小尺寸连拍 / 联系表
   - 胶带固定的照片
   - 带日期、地点或一句批注的照片

   照片尽量保留真实摄影质感，不统一套同一种重滤镜。

   ### 4.4 纸张与文具元素

   可使用：

   - 便利贴
   - 撕边纸
   - 方格纸片
   - 半透明胶带
   - 回形针
   - 票据与标签
   - 印章
   - 荧光笔或彩铅标记
   - 小型手绘插图
   - 箭头、括号、圈画和连接线

   这些元素必须服务于内容和节奏，不应成为无意义装饰。

   ## 5. 首页信息与栏目入口

   首页栏目保持以下映射：

   - 主要文章记录区域 -> `Blog`
   - 便利贴 -> `Thoughts`
   - 佳能 A-1 胶片相机 -> `Photos`
   - 伊势角屋「小猫三只」 -> `Drinks`
   - 简历相框 -> `About Me`
   - 杂志架 -> `Books`
   - 黑胶唱片架 -> `Music`
   - 名字叫“包子”的复古小机器人 -> `AI Works`

   以上只规定语义映射，不预先锁死图片、比例、文案和最终坐标。具体物件图片与文案由用户后续提供，再根据真实素材进行编排。

   ## 6. 首页欢迎语

   左上区域显示基于访问者时区的招呼语，例如：

   - 早上好，欢迎光临中华一番包子铺
   - 下午好，欢迎光临中华一番包子铺
   - 晚上好，欢迎光临中华一番包子铺

   时间判断优先使用浏览器时区。只有在确实需要地区级文案时，再考虑 IP 定位服务，避免不必要的权限、隐私和网络依赖。

   欢迎语应像手账的开篇记录，不做成传统网站 Header 或大型 Banner。

   ## 7. 交互模型

   ### 7.1 可拖动物件

   优先支持拖动：

   - 便利贴
   - 白色描边照片
   - 拍立得照片
   - 小纸签
   - 部分贴纸或标签

   约束：

   - 拖动范围优先限制在当前跨页。
   - 拖动不能遮住核心导航或正文。
   - 可在刷新后恢复初始位置，首版不强制持久化用户布局。
   - 移动端需区分拖动与页面滚动，避免手势冲突。
   - 应尊重 `prefers-reduced-motion` 与触控设备性能。

   ### 7.2 点击与翻页

   推荐流程：

   1. 用户点击栏目物件或标签。
   2. 物件获得轻微聚焦、抬起或印章式反馈。
   3. 页面短暂显示栏目标题。
   4. 触发翻页。
   5. 进入对应栏目跨页或内容页。

   翻页应模拟纸张逻辑，但以可用性和性能优先，不追求复杂 3D 物理模拟。

   ### 7.3 可交互线索

   栏目入口可通过以下方式提示可点击：

   - 轻微 hover 抬起
   - 白色描边阴影变化
   - 手写箭头或圈画
   - 小型英文标签
   - 页角或纸签状态
   - 鼠标移动时的微小位移

   不使用传统大按钮、卡片按钮或电商式 CTA。

   ## 8. 信息架构

   ### Blog
   完整文章、教程、经验总结和专题内容。

   ### Thoughts
   短想法、观察、灵感和未完成念头。

   ### Photos
   摄影、生活记录、主题相册和视觉片段。

   ### Drinks
   喝过的酒、酒款、场景和主观评价。

   ### Books
   书籍、书摘、推荐和阅读记录。

   ### Music
   专辑、歌单、单曲和听感记录。

   ### About Me
   个人介绍、当前状态、关注主题、联系方式和社交链接。

   ### AI Works
   AI 图像、视频、文字实验和生成作品。

   ## 9. 内容模型

   ```text
   content/
     blog/
     thoughts/
     photos/
     drinks/
     books/
     music/
     about/
     ai-works/
   ```

   每类内容至少包含：

   - 标题或名称
   - 日期
   - 简短摘要
   - 标签
   - 封面或主图（可选）
   - 正文或描述
   - 外部链接（可选）
   - 状态（适用于 AI 作品或实验）

   ## 10. 素材工作流

   用户将逐步提供：

   - 各栏目对应物件的真实图片
   - 首页欢迎语与栏目文案
   - 手写批注
   - 照片
   - 印章与手绘素材
   - 需要出现的品牌、书籍、唱片和酒款信息

   素材进入网站前建议统一处理：

   ```text
   原始素材
     -> 抠图 / 裁切
     -> 白色描边或拍立得处理
     -> 导出透明 PNG / WebP
     -> 生成缩略图
     -> 上传至图片存储
     -> 写入内容与布局配置
   ```

   建议每个物件保留：

   - 原图
   - 透明底主图
   - 白色描边版本
   - 缩略图
   - 移动端低分辨率版本

   ## 11. 响应式方向

   ### 桌面端

   > 2026-07-24 更新：首页已改为单页平铺（见 3.5），以下双页相关描述已过时。

   - ~~以完整双页跨页为基准。~~（已过时，见 3.5）
   - 手账页面铺满主要视口。
   - 可保留中缝与轻微纸边。
   - 采用自由编排，避免规则栏目矩阵。
   - 支持 hover、拖动和翻页。

   ### 移动端

   > 2026-07-24 已决策：≥ 660px 等比缩放整张纸（transform: scale，构图不变；物件在 1200 设计坐标系下足够大，缩放后仍可达）；< 660px 纵向重排四入口（≥ 44px，装饰精简）。原「不简单缩小」的目标不变，实现路径以此为准，以下为历史备选。

   移动端在 660px 以上整体等比缩放，660px 以下纵向重排。

   优先方案：

   - 单页手账视图
   - 纵向连续手账
   - 左右滑动切换页
   - 轻量页签或纸边索引

   需保留：

   - 白色点阵纸
   - 手写与贴纸语言
   - 真实物件比例
   - 点击栏目后翻页的感觉

   ## 12. 性能与可访问性

   - 首屏素材按重要性分级加载。
   - 图片优先使用 WebP / AVIF，并保留合理尺寸。
   - 非首屏图片使用懒加载。
   - 拖动与翻页避免依赖高成本持续动画。
   - 对减少动态效果的用户提供简化版本。
   - 所有栏目入口必须提供可聚焦、可键盘操作的语义链接。
   - 不能仅靠拖动或图像辨识完成导航。
   - 手写文字重要信息需提供可读取的文本层。

   ## 13. 当前设计基准

   当前确认的首页概念图：

   > `温馨的手账首页设计.png`

   该图只用于确认：

   - 白色点阵 / 浅方格背景
   - 无限延展的打开手账
   - 手账主导整个视口
   - 自由而非矩阵化的版式
   - 较小的物件比例
   - 手写、标签、照片与印章混排的气质

   图中的具体物件图片、文案、日期和摆放均不是最终版本。后续以用户提供的真实素材和文案为准。

   ## 14. 当前阶段与下一步

   当前阶段：

   > 视觉背景、纸张语言和版式原则已经确认；具体内容与物件尚待用户提供。

   接下来按以下顺序推进：

   1. 收集首页八个栏目所需的具体物件图片。
   2. 收集欢迎语、栏目标题、说明文字和手写批注。
   3. 整理照片、印章、贴纸和辅助素材。
   4. 建立首页素材清单与尺寸规范。
   5. 完成桌面端首页跨页第一版。
   6. 完成移动端单页 / 连续手账方案。
   7. 验证拖动、点击和翻页交互。
   8. 再进入各栏目内页模板设计。

   ## 15. 当前结论

   已确认的方向为：

   > 「中华一番包子铺」不是一个套用手账风格的网页，而是一本文档尺度近乎无限延展的数字手账。页面以白色点阵或浅方格纸为基础，使用真实手账式的灵活编排；纸张与书写空间是主角，照片、便利贴、印章、手绘和物件以较小比例自然分布。后续所有具体物件和文案由用户提供，再据此完成最终首页编排。
