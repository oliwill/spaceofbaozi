# baozi.space 混合动画设计规格（历史）

> **本规格已废止（D-121，2026-08-24）。** 其描述的「5–7 秒整屏 Seedance 视频正常播放、末帧交接 Home v2」路线已被 oil-motion 帧映射方案取代，三条成片因背景污染与方向错误被拒收。当前生产方向见根目录 `2026-08-21-baozi-space-oil-motion-project-plan.md` 与 `docs/animation/oil-motion/`。本文仅保留历史，不再作为执行入口。

状态：已废止，仅作历史参考（D-121）  
日期：2026-08-18  
适用版本：B-lite 启动动画与 Home v2

## 1. 目标

将当前开场改造成一段动作连续、角色稳定的电影化动画，并在结束时无缝进入可交互的 Home v2。开场追求预渲染视频的流畅度，首页保留实时交互、可访问性和静态内容能力。

## 2. 已确认的技术路线

- 站点继续使用 Astro 5、TypeScript、Markdown Content Collections、原生 CSS 与 Bun。
- GSAP/ScrollTrigger 继续负责页面状态、触发、显隐与降级，不再逐帧驱动开场人物动作。
- Seedance 2.5 优先用于制作 5–7 秒开场视频；不可用时使用 Seedance 2.0。
- 开场视频正常向前播放；第一次滚轮、触摸滑动、键盘翻页或点击用于触发，不把滚动位置映射为视频 `currentTime`。
- Home v2 的人物与嘉乐使用 Rive Web Runtime 做实时互动；若 Rive 样片无法通过水彩质感门禁，则采用 16 方位、统一枢轴的 WebP Sprite 方案。
- 不引入 Three.js。
- HTML 文案、按钮、导航和首页内容均保留为 DOM，不烘焙进视频。

## 3. 体验流程

1. 首次访问显示开场视频的第一帧与“跳过动画”。
2. 用户第一次明确输入后，页面固定并播放 5–7 秒无声视频。
3. 视频结尾定格必须与 Home v2 的初始状态一致。
4. Home v2 与互动角色在视频播放期间于后台准备。
5. 视频结束时切换到 Home v2；若两端像素匹配，不使用可见淡化。
6. 页面恢复滚动，小狗只在自动入场半圈或明确的鼠标、触摸、键盘输入后移动。
7. 本次会话再次访问时直接显示 Home v2。

## 4. 视觉基线

- 主背景：无限延展的暖白低对比点阵纸。
- 场景下方：约占视口四分之一的 B-lite 水彩草地。
- 主色：绿、白、蓝；黄色只用于小球等小面积强调。
- 人物：渔夫帽、夏季短袖短裤造型，保留水彩/印刷斑驳感与清晰白色描边。
- 嘉乐：白色比熊犬，轮廓、蓝色项圈、体型和脸部特征全程一致。
- 镜头：固定机位；禁止自动推拉、摇移、景深变化和背景呼吸。
- 页面文案以中文为主，不在视频中生成任何文字。

## 5. 开场动作

建议成片时长 6 秒，允许在 5–7 秒范围内微调：

| 时间 | 动作 |
| --- | --- |
| 0.0–0.7s | 黄色小球从左侧进入草地并滚动 |
| 0.7–1.8s | 嘉乐从左侧追球进入 |
| 1.8–3.3s | 牵引绳拉紧，人物被带入画面 |
| 3.3–4.6s | 人物与嘉乐向镜头方向接近，透视逐渐增强 |
| 4.6–5.2s | 人物失衡并撞向/摔出画面 |
| 5.2–6.0s | 暖白纸张显现，人物站起，嘉乐落到 Home v2 初始位置 |

牵引绳属于必须后期检查的高风险元素。生成视频中的绳子只作为参考；成片必须确保绳端稳定连接人物握持点和嘉乐项圈，线条不闪烁、不穿模、不改变长度逻辑。

## 6. Home v2 互动

- 轨道为二维椭圆，纵横比基线 0.45。
- 嘉乐位置、缩放和层级由同一个连续角度计算。
- 角度必须使用最短路径插值，并限制角速度与角加速度。
- 步态相位由沿轨道移动的距离驱动，不由独立定时器驱动。
- 人物身体保持正面或最多左/中/右三个轻微姿态；注视主要通过头部和上半身完成。
- 禁止对完整人物立绘做频繁的 100ms 交叉淡化。
- 静止后停止动画循环和持续几何读取。

## 7. 视频交付规格

| 项目 | 桌面端 | 移动端 |
| --- | --- | --- |
| 画幅 | 16:10 | 9:16 |
| 时长 | 5–7 秒 | 与桌面版动作一致 |
| 帧率 | 24 或 30fps，最终只保留一种 | 与桌面一致 |
| 声音 | 无音轨 | 无音轨 |
| 首选格式 | WebM（VP9） | WebM（VP9） |
| 回退格式 | MP4（H.264） | MP4（H.264） |
| 背景 | 完整合成，不依赖 Alpha 视频 | 完整合成，不依赖 Alpha 视频 |

浏览器只下载匹配视口和格式的一份视频。桌面端首选视频目标不超过 4.5MB，移动端不超过 3MB；开场关键资源总负载继续遵守不超过 6MB 的门禁。

## 8. 状态与降级

- `prefers-reduced-motion: reduce`：不播放视频，直接显示视频末帧和 Home v2。
- `sessionStorage` 已完成标记：直接进入 Home v2。
- 用户点击“跳过动画”：停止媒体、写入完成状态、显示同一 Home v2。
- 视频加载失败、超时或解码失败：显示视频末帧，再进入 Home v2。
- Rive 加载失败：显示生产级静态人物与嘉乐定帧，不阻塞首页内容。
- JavaScript 禁用：首页的文字、导航与内容仍可读取。

## 9. 关键一致性规则

- 视频最后一帧与 Home v2 第一帧使用同一张批准的 `handoff-final` 基准图。
- 两端人物脚底、身体尺度、嘉乐位置、点阵纸原点、纸张色温和裁切方式必须一致。
- 不允许视频结束后角色突然缩放、平移、换衣服、换姿势或改变描边。
- `/lab/intro` 不再包含另一套占位首页；实验路由和生产路由共用同一个 Home v2 组件。
- D-105 旧首页保留为可回滚版本，但不再作为新开场的落点。

## 10. 完成定义

- 首次访问可完整播放并进入 Home v2，无黑帧、白闪和布局跳变。
- 视频最后一帧到 Home v2 第一帧肉眼不可见跳变。
- 嘉乐环绕没有角度跳跃、重影和明显步态滑动。
- 人物注视自然，不再整身闪烁或改变重心。
- 桌面、移动端、跳过、会话、减弱动态和失败降级均通过自动化测试。
- Astro check、Vitest、Playwright、静态构建和资产审计全部通过。
