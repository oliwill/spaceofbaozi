# baozi.space PRD

**产品名：** 中华一番包子铺  
**文档状态：** 可执行 PRD；以现有 Astro 静态站为生产基线  
**更新时间：** 2026-07-24

## AI 速读卡

- **产品一句话：** 一本会持续变厚的数字手账，让访客以翻开、翻阅和发现的方式认识包子的文章、生活记录与 AI 创作。
- **核心循环：** 进入首页开本 → 点击带语义的手账物件 → 浏览栏目与条目 → 阅读详情或访问外链 → 创作者以 Markdown 增添下一页。
- **目标平台：** 静态 Web；桌面单页平铺手账，移动端等比缩放或纵向重排。
- **硬约束：** 八个栏目、Markdown 为内容源、所有导航可键盘完成、草稿不可公开、尊重减少动态效果。
- **推荐默认：** 沿用 Astro 5、Content Collections、原生 CSS/JS 与本地中文字体子集。
- **发挥空间：** 手写批注、物件排布、轻量纸张过渡、空状态与栏目页面的编辑感。
- **P0 验收：** 首页当前入口（Blog、Thoughts、Photos、About）均可访问，其余栏目 URL 可直达；非草稿条目可列表/详情阅读；草稿 URL 返回 404；移动端可不依赖拖动完成导航。
- **最容易翻车：** 把手账做成不易操作的装饰；用复杂 3D 动画牺牲访问、性能和可访问性。
- **超预期机会：** 用真实素材生成的“最近翻到这一页”首页更新贴纸；每篇内容的手写阅读痕迹。

## 第一章：产品概述

**定位：** 中华一番包子铺是一款数字手账式个人网站，让对包子感兴趣的访客能探索其文章、想法、摄影、酒、书、音乐与 AI 创作，而无需在传统博客、作品集和社交平台之间拼凑其个人表达。

### 1.1 差异化对比表

| 功能 | 传统静态博客 / Gridea 式站点 | 作品集模板 | 中华一番包子铺 | 实现方式 |
|---|---|---|---|---|
| 首次入口 | 文章流或导航栏优先 | Hero 与项目卡优先 | 先打开一本手账，再通过物件进入内容 | 轻量开本状态与语义链接叠加 |
| 内容组织 | 以单一文章时间线为主 | 以案例集合为主 | 八类生活与创作内容并列，保留各自语境 | 八个 Content Collections + 栏目元数据 |
| 浏览情绪 | 阅读效率优先 | 商业展示优先 | “翻到一页记录”的发现感，但不牺牲可读性 | 白色点阵页、贴纸、批注、真实素材 |
| 维护路径 | 后台或文件维护 | 常需 CMS / 手工改页面 | 在 Obsidian/编辑器中直接维护 Markdown，Git 自动发布 | `src/content` 作为生产内容源 |
| 访问降级 | 通常不涉及沉浸动画 | 通常仅响应式缩放 | 动效、拖动只增强体验；链接与文字始终是主路径 | 原生链接、键盘焦点、`prefers-reduced-motion` |

### 1.2 三类用户画像

1. **偶然来访者——阿岚，25 岁，设计与创作爱好者**
   - 核心目标：在三分钟内理解“包子是谁、在关注什么、哪些内容值得继续看”。
   - 最大不满：标准个人站视觉漂亮但信息像履历，难以感到人的日常与审美。
   - 切换理由：从首页物件自然进入照片、书、音乐和文章，先感知人，再决定深读。

2. **长期读者——小周，29 岁，订阅/收藏型读者**
   - 核心目标：快速发现最近更新、按栏目重访、稳定阅读长文与短想法。
   - 最大不满：社交平台内容流被算法打断，旧内容难回找。
   - 切换理由：栏目页按日期稳定归档，内容 URL 可直接分享、收藏和回访。

3. **创作者本人——包子**
   - 核心目标：低摩擦地把新文章、照片与灵感放进长期可迁移的个人档案。
   - 最大不满：可视化建站工具维护方便但内容数据和版式控制受限；纯代码维护又太重。
   - 切换理由：新内容只需新增 Markdown；图片可使用普通 HTTPS 图床 URL；发布前自动重建中文字体子集。

### 1.3 可行性边界

| 在范围内（及原因） | 明确排除在外（及原因） |
|---|---|
| 静态 HTML 内容站、SEO 元数据、可分享的栏目与详情 URL：适合 Astro 静态输出。 | 用户账号、评论、私信、在线编辑：P0 不验证社区关系，且会引入鉴权、垃圾内容与数据治理。 |
| 浏览器本地时间招呼语：无需收集 IP、定位或个人数据。 | 以 IP 推断所在地：不解决核心体验，增加隐私、可靠性和网络依赖。 |
| 少量可拖动纸片：只作为桌面端增强，不保存用户布局。 | 自由画布、多人协作和持久布局：会把内容站变成编辑器，偏离核心循环。 |
| 轻量栏目过渡、纸张视觉和减少动态效果降级：可在无框架前端完成。 | 高成本连续 3D 翻页物理模拟：性能和可访问性收益不匹配。 |
| Markdown、Git、图片 URL 与静态部署：内容可备份、迁移和版本化。 | 在站内处理原始图片抠图/图像生成：应在素材工作流中完成，不放入访问路径。 |

### 1.4 约束分层

| 硬约束 | 推荐默认 | 发挥空间 |
|---|---|---|
| 网站必须是“手账即页面”的内容体验，不可退化成八宫格应用入口。 | 沿用 Astro 5 + Markdown Content Collections + 静态输出。 | 可调整物件具体坐标、纸张细节、批注文案和微动效。 |
| 八栏目为 Blog、Thoughts、Photos、Drinks、Books、Music、About、AI Works。 | 桌面使用单页平铺网点纸（2026-07-24 起取代双页跨页），移动端 ≥660px 等比缩放、<660px 纵向重排。 | 可为每个栏目定义不同的纸张编排与封面表现。 |
| 栏目入口必须是可聚焦、可读标签的链接；图片/拖动不能是唯一导航。 | 点击物件后先显示短栏目过渡，再导航；减少动态效果时立即导航。 | Hover、抬起、印章、纸边等反馈可按素材和性能选择。 |
| `draft: true` 内容不可经列表或详情 URL 公开。 | 内容用日期倒序，卡片最多展示三个标签。 | 条目精选、阅读关联、首页“最近更新”陈列可逐步增加。 |
| 不采集访问者身份、位置或行为分析数据作为 P0 条件。 | 首页欢迎语由浏览器本地小时计算。 | 可在明确隐私说明后增加匿名、无 Cookie 的访问统计。 |

## 第二章：整体布局与导航

### 2.1 信息架构

```text
baozi.space
+-- 首页 /
|   +-- 开本状态
|   +-- 左页：欢迎语、Blog、Thoughts、Photos、Drinks
|   +-- 右页：About、Books、Music、AI Works
|   +-- 栏目标题过渡层
+-- /blog          +-- /blog/<slug>
+-- /thoughts      +-- /thoughts/<slug>
+-- /photos        +-- /photos/<slug>
+-- /drinks        +-- /drinks/<slug>
+-- /books         +-- /books/<slug>
+-- /music         +-- /music/<slug>
+-- /about         +-- /about/<slug>
+-- /ai-works      +-- /ai-works/<slug>
```

### 2.2 首页布局

```text
桌面端：约 100vw × 100vh，手账占主要视口
+--------------------------------------------------------------------------------+
| 合上状态：FREITAG F26 AGENDA 封面                                               |
|                     “中华一番 / 包子铺”                                        |
+--------------------------------------------------------------------------------+
                             ↓ 自动打开或按 Enter/点击打开
+--------------------------------------------------------------------------------+
| 左页（约 49%）                         || 右页（约 49%）                      |
| 早上好，欢迎光临中华一番包子铺          || baozi.space · 2026 · 07 · 24          |
|                                        ||                                       |
| Blog · 最近更新的阅读卡                   || About Me · 简历相框                  |
| Thoughts · “突然想到……”便利贴            || Books · 六月读完的杂志架              |
| Photos · 胶片相机 + 拍立得                || Music · 循环中 ↻ 黑胶唱片架           |
| Drinks · 小猫三只啤酒贴纸                 || AI Works · 包子的小机器人             |
|                                        ||                                       |
| 白纸、浅点阵、少量胶带/批注、可拖动纸片  || 白纸、浅点阵、少量胶带/批注              |
+--------------------------------------------------------------------------------+
| ↻ 重新打开（非关键辅助操作）                                               |
+--------------------------------------------------------------------------------+
```

桌面端保留单页平铺网点纸与自由拼贴，服务“发现”的首印象；移动端以阅读和点击优先，先等比缩放、过窄则纵向重排。

```text
移动端：单列连续手账
+----------------------------------+
| 中华一番 · 包子铺                  |
| 下午好，欢迎光临                    |
| Blog · 最近更新                      |
| Thoughts · 突然想到……                |
| Photos · 胶片与生活                   |
| Drinks · 小猫三只                     |
| Books · 阅读架                        |
| Music · 循环中 ↻                      |
| About · 关于包子                       |
| AI Works · 创作实验                    |
+----------------------------------+
```

### 2.3 全站导航流程

```text
访问 /
  |
  +-- 用户启用减少动态效果 ----------> 立即展示打开状态
  |
  +-- 默认 --------------------------> 450–900ms 轻量开本 → 展示双页
                                                          |
                                                          v
                      点击 / Enter 激活任一栏目语义链接
                                                          |
          +------------------------------+------------------------------+
          |                              |                              |
          v                              v                              v
      显示栏目名过渡                 JS 不可用                       链接/路由失败
          |                              |                              |
          v                              v                              v
      进入 /<section>                浏览器原生跳转                 保留地址，显示 404 页面
          |
          v
      日期倒序列表 → 点击条目 → /<section>/<slug> → 阅读/外链
```

## 第三章：核心模块详细设计

### 第 3.1 节 首页开本与栏目入口

> 2026-07-24 起首页基线改为**单页平铺网点纸**（见 `baozi-space-spec.md` §3.5）；本节双页跨页图示为已过时基线，待真实素材到位、首页重建时重绘。

#### a) ASCII 图

```text
默认（合上）
+--------------------------+
|   中华一番                |
|      包子铺               |
|  浅薄荷蓝篷布封面         |
+--------------------------+

打开后（可交互）
+-----------------------------------------+
| “下午好，欢迎光临中华一番包子铺”        |
| 最近更新 · Blog                         |
| Thoughts · 便利贴     Photos · 相机     |
| Drinks · 啤酒        Books · 杂志架     |
| Music · 黑胶         AI Works · 机器人  |
|                    About Me · 相框      |
+-----------------------------------------+

错误/降级
+-----------------------------------------+
| 中华一番包子铺                            |
| Blog · Thoughts · Photos · Drinks         |
| Books · Music · About Me · AI Works       |
| 动效未启用，栏目链接仍可访问              |
+-----------------------------------------+
```

#### b) 交互流程

```text
正常：访问首页 → 检查减少动态效果 → 开本 → 聚焦第一个栏目入口 → 点击/Enter → 栏目过渡 → 对应栏目页

失败路径 1：动画资源或 JS 初始化失败 → 不阻塞语义 <a> → 浏览器直接打开 href → 到达对应栏目页。
失败路径 2：指针拖动越过阈值后松开 → 仅更新纸片临时位置 → 不触发栏目跳转；下一次点击才能进入。
失败路径 3：触控屏纵向滑动 → 优先滚动页面 → 不将滚动误判为拖动。
```

#### c) 状态清单

| 状态 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 合上 | 首次加载且未启用减少动态效果 | 封面可见，双页 `aria-hidden` | 开本动画结束、用户触发打开，或降级策略生效 |
| 开本中 | 初始化动画启动 | 封面缩退、页面展开 | 动画结束或 900ms 超时保护 |
| 已打开 | 动画结束 | 双页、物件、栏目链接可操作 | 进入栏目、重新打开 |
| 物件拖动中 | 桌面指针移动超过拖动阈值 | 纸片跟随指针且不跳转 | 指针释放或取消 |
| 栏目过渡 | 入口被点击 | 栏目编号、标题、描述覆盖页 | 进入路由、返回或超时 |
| 降级 | `prefers-reduced-motion` 或 JS 不可用 | 静态栏目文本/链接 | 无；浏览器原生导航 |

#### d) 依赖关系

```text
浏览器时间 ──读──> greetingByHour ──写──> 欢迎语
sections.ts ──读──> 首页栏目标签 / public/scripts/journal.js 跳转信息
首页语义链接 ──写──> 浏览器路由 ──读──> 栏目页
可拖动纸片 ──只读/临时状态──> DOM transform（不写入用户数据）
```

#### e) 待决问题

- 此处未解决：真实首页物件素材何时齐备，以及每个素材的桌面/移动端裁切版本。
- 已决策（2026-07-24）：首页物件分「入口」与「装饰」，入口名单显式维护（当前 Blog、Thoughts、Photos、About），装饰不可点击；栏目内容充实后手动晋升为入口。
- ~~此处未解决：首页是否只展示人工精选的“最近更新”，还是从八个栏目自动汇总一条最新内容~~（2026-07-27 已解决：首页自动汇总全站非草稿条目最新 3 条，为普通文字链接，不占物件入口）。

### 第 3.2 节 栏目归档页

#### a) ASCII 图

```text
+------------------------------------------------------------------------+
| baozi.space · 中华一番包子铺                    ← 返回手账首页         |
| 01 · 文章记录                                                         |
| Blog                                                                   |
| 经验、教程与专题内容。                                                  |
|------------------------------------------------------------------------|
| 2026/07/23  手账为什么不能像一个仪表盘                  #设计 #网站   |
| 手账感不是贴纸堆砌，而是阅读路径与时间痕迹……                           |
|------------------------------------------------------------------------|
| 2026/07/20  一次小小的摄影练习                            #胶片       |
| 记录当天光线、器材和回看后的判断……                                   |
+------------------------------------------------------------------------+

空状态
+------------------------------------------------------------------------+
| 手账这一页暂时空白。                                                   |
| 下一篇记录正在整理；先从首页翻到其他栏目。 返回手账首页                 |
+------------------------------------------------------------------------+
```

#### b) 交互流程

```text
正常：进入 /books → 读取 books 集合中非草稿条目 → 日期倒序 → 显示标题、日期、最多 3 标签、可选封面 → 点击条目。

失败路径 1：集合为空 → 显示栏目语境化空状态与回首页链接，不显示空白卡片墙。
失败路径 2：封面 URL 失效 → 浏览器显示 alt 文本且卡片正文仍可读、可点击；不阻塞列表。
失败路径 3：直接访问不存在 slug → 由静态路由返回 404，不伪造空详情。
```

#### c) 状态清单

| 状态 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 有内容 | 至少一条 `draft: false` | 日期倒序条目卡 | 进入详情或内容被移为草稿 |
| 空栏目 | 没有公开条目 | “这一页暂时空白”与回首页 CTA | 新公开条目被构建 |
| 图片缺失 | `cover` 网络加载失败 | alt 文本/浏览器失败图标，正文卡保持可用 | 刷新成功或内容修复 |
| 404 | 路由不存在或草稿详情直达 | 网站级 404 | 返回首页/有效 URL |

#### d) 依赖关系

```text
src/content/<section>/*.md
  └─读→ Astro Content Collection（schema 校验）
       └─读→ /<section>/index.astro
            └─写→ 条目链接 /<section>/<slug>
                 └─读→ 对应详情路由
```

#### e) 待决问题

- 需要内容量增长后再决定是否引入标签筛选；P0 不加入筛选，避免每个栏目因少量条目而出现伪复杂控件。
- 此处未解决：Photos 是否采用与其他栏目相同的文字卡片，还是在条目达到至少 12 个后增加轻量瀑布流；默认沿用一致列表，先确保可访问与可维护。

### 第 3.3 节 条目详情与外部去向

#### a) ASCII 图

```text
+------------------------------------------------------------------------+
| baozi.space · 中华一番包子铺                    ← 返回 Books           |
| 2026/07/18 · #阅读 · #笔记                                            |
| 读《网页作为房间》时记下的三件事                                       |
| 一段简介，说明这页记录为什么值得留下。                                 |
|------------------------------------------------------------------------|
| 正文                                                                    |
| 真实段落、图片、图注与链接。                                            |
|                                                                        |
| 如果存在 link：在新标签阅读原文 ↗                                      |
|------------------------------------------------------------------------|
| 继续往下翻，这本手账还会更厚。                                          |
+------------------------------------------------------------------------+
```

#### b) 交互流程

```text
正常：访问固定 URL → 查找对应 collection/id 的非草稿内容 → 渲染标题、时间、标签、正文、图片 → 阅读或访问可选外链。

失败路径 1：内容被设为 `draft: true` 后仍访问旧 URL → 返回 404，不能泄露标题、摘要或正文。
失败路径 2：外链不可达/跳转失败 → 新标签页自行显示失败；本站详情不丢失、返回链接可用。
失败路径 3：正文图片加载失败 → 图像 alt 文本可见，正文继续布局，不显示遮挡性弹窗。
```

#### c) 状态清单

| 状态 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 正常阅读 | 匹配非草稿条目 | 元数据、标题、正文、图片 | 返回栏目或跟随外链 |
| 带外链 | `link` 存在且通过 URL schema | 明确的外链标记 `↗` | 用户打开新标签 |
| 缺图 | Markdown 图片请求失败 | 可读 alt 与保留排版空间 | 资源恢复或编辑内容 |
| 不可见 | 条目不存在、草稿或集合不符 | 404 页面 | 进入有效路径 |

#### d) 依赖关系

```text
Markdown frontmatter ──读──> title/date/tags/cover/draft/status/link
Markdown body ──读──> 内容渲染器 ──写──> HTML 正文
详情路由 ──校验──> collection + slug + !draft
外链字段 ──只读──> 明确的新标签出口（若存在）
```

#### e) 待决问题

- 此处未解决：长文是否需要目录；默认仅在单篇正文超过 1,500 个中文字符且 H2 数量不少于 4 时设计目录，避免短内容增加噪音。
- 正式外链策略未确定；默认使用 `target="_blank"` 并提供可读外链提示，是否追加 `rel="noopener noreferrer"` 应作为实现约束执行。

### 第 3.4 节 内容维护、预览与发布

#### a) ASCII 图

```text
+----------------------------------------------+
| Obsidian / 编辑器                              |
| src/content/blog/my-new-note.md               |
| title · date · tags · draft: true             |
+-------------------------+--------------------+
                          |
                          v
+----------------------------------------------+
| 本地校验                                     |
| bun run check  →  bun run build               |
+-------------------------+--------------------+
                          |
             +------------+------------+
             |                         |
             v                         v
       校验失败：修正 frontmatter   成功：设 draft: false
                                           |
                                           v
+----------------------------------------------+
| bun run publish                               |
| 子集化中文字体 → 提交 → 推送 → 静态部署        |
+----------------------------------------------+
```

#### b) 交互流程

```text
正常：复制栏目模板 → 填写 frontmatter/正文 → 保持 draft: true → 本地检查与预览 → 改为 false → 执行发布脚本 → 部署平台构建静态站。

失败路径 1：日期、link 或 YAML 类型不符合 schema → `astro check`/build 报错 → 不发布，修正源 Markdown。
失败路径 2：新增中文文案但未子集化字体 → 发布脚本先执行 fonts:subset；若脚本失败，提交与推送不应继续。
失败路径 3：图片 CDN 不可访问 → 页面正文仍可发布，但发布前应在本地预览检查图片；长期将失效 URL 视为内容缺陷处理。
```

#### c) 状态清单

| 状态 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 草稿 | `draft: true` | 列表与详情不生成公开入口 | 改为 `false` 并重新构建 |
| 可预览 | 本地开发服务运行 | `127.0.0.1:4321` 页面可查看 | 停止服务或发布 |
| 校验失败 | schema/类型/构建出错 | 终端明确错误 | 修正源文件重新执行 |
| 可发布 | check 与 build 成功 | 静态输出生成 | 执行发布命令 |
| 已部署 | 推送触发成功构建 | 生产地址更新 | 下次内容提交 |

#### d) 依赖关系

```text
Obsidian Junction / 编辑器
  └─写→ src/content/*.md
      └─读→ Astro schema / 路由生成
          └─读→ fonts:subset（扫描中文字符）
              └─写→ public/fonts/*.woff2
                  └─读→ 静态构建与部署
```

#### e) 待决问题

- 此处未解决：Cloudflare Pages 控制台连接、首次部署和 `baozi.space` 域名绑定尚需完成；PRD 不把它当作已上线事实。
- 此处未解决：PicGo 的最终图床后端；P0 只要求可引用 HTTPS 图片 URL，不绑定任一厂商。

## 第四章：超越竞品的差异化功能

### 第 4.1 节 语义优先的可探索手账首页

1. **竞品为何没有这个功能：** 基于公开产品形态的推断，传统博客的信息架构从文章索引出发，作品集模板则从项目转化出发；它们用稳定栅格降低维护成本，因此很少将“可探索的物件入口”与可访问的正文导航同时作为首层结构。
2. **本产品如何实现：** 每个物件必须包含真实文本标签和 `<a href>`，CSS/JS 仅叠加开本、抬起、过渡与拖动。推荐链接覆盖物件与标签的可点击区域，拖动仅赋予明确的纸片对象。
3. **交互流程：**

```text
视觉物件 + 可读栏目名
     |
     +-- 鼠标悬停 → 轻微抬起/批注强调
     +-- 键盘 Tab → 可见焦点 → Enter → 进入栏目
     +-- 减少动态效果 → 直接显示链接并导航
```

4. **风险与应对：** 复杂绝对定位容易让焦点顺序与视觉顺序脱节。DOM 顺序必须按阅读顺序排列；每次调整首页物件后，以键盘 Tab 完整走一遍全部当前入口，并在 390px 窄屏检查无重叠遮挡。

### 第 4.2 节 内容与设计不会互相锁死

1. **竞品为何没有这个功能：** 基于公开产品形态的推断，可视化建站工具通常把内容、页面块和托管工作流绑定在平台内；高度定制的个人站又常把每篇内容硬编码入组件，导致维护门槛上升。
2. **本产品如何实现：** 用统一 Markdown schema 提供内容最小契约，再由栏目元数据和通用列表/详情模板生成页面。首页只引用栏目语义，不依赖某篇内容存在。
3. **交互流程：**

```text
新增 Markdown
  → schema 校验
  → 非草稿内容进入列表/详情
  → 首页仍保持稳定手账布局
  → 内容逐渐让手账“变厚”
```

4. **风险与应对：** 通用 schema 会限制 Photos、Drinks 等特殊内容的表达。P0 保持共用字段；当某个栏目连续出现至少 5 次同类附加字段时，再以显式、向后兼容的可选字段扩展，而不是在正文里解析非结构化约定。

### 第 4.3 节 有边界的个人化欢迎与纸张记忆

1. **竞品为何没有这个功能：** 基于公开产品形态的推断，许多站点为了分析和个性化会接入追踪脚本；个人站的“欢迎”常变成静态 Hero，缺少轻微的时间感。
2. **本产品如何实现：** 只读取浏览器本地小时，生成早上/中午/下午/晚上/夜深的招呼语；日期印章从本机日期填充。不上传身份、位置或行为数据。
3. **交互流程：**

```text
浏览器本地时间 → 计算招呼语和日期印章 → 写入首页可读文本
       |
       +-- 时间 API 不可用/JS 不可用 → 使用 SSR 初始招呼或静态文案
```

4. **风险与应对：** SSR 时区与访客时区可能不同。服务器只提供不阻塞的初始文案；页面加载后由客户端纠正。重要内容不得依赖该文案。

### 4.4 超预期机会

- **一张“最近翻到这一页”更新贴纸：** 当内容量稳定后，首页只展示一条人工挑选或自动汇总的最新公开条目。它必须是普通链接，不能替代栏目入口。
- **真实素材的两级处理：** 主图使用抠图白边贴纸，详情页保留摄影原貌/拍立得图注。相同内容既有首页辨识度，也不破坏原始照片。
- **阅读痕迹而非点赞计数：** 长文末尾可放“本页关键词/当时记录”手写批注。它是内容编辑，不做账户、点赞或行为追踪。
- **跨页回到手账：** 从详情返回栏目/首页时保留清晰的“返回手账首页”链接；若未来实现过渡，仍以浏览器后退和 URL 可预测性为前提。

## 第五章：数据模型

```jsonc
{
  "version": 1, // 内容模型版本；必填
  "entry": {
    "title": "网页作为房间", // 必填；条目标题，1–120 个字符
    "description": "关于网页空间感的一次笔记。", // 默认值: ""；列表摘要
    "date": "2026-07-24", // 必填；ISO 日期，用于排序与展示
    "tags": [], // 默认值: []；示例：网站、设计；建议 0–5 个短标签
    "cover": "https://images.example.com/web-as-room.webp", // 可选；HTTPS 封面 URL
    "draft": true, // 默认值: false；true 时不得生成公开列表或详情
    "status": "published", // 可选；适合 AI Works 的实验状态，不用于访问控制
    "link": "https://example.com/original", // 可选；合法 URL，指向外部原文/作品
    "featured": false // 默认值: false；预留给人工精选，不改变时间排序
  },
  "section": {
    "key": "blog", // 必填；固定为八个栏目 key 之一
    "number": "01", // 必填；栏目视觉编号
    "title": "Blog", // 必填；展示名称
    "zh": "文章记录", // 必填；中文栏目名
    "href": "/blog", // 必填；站内稳定路由
    "collection": "blog" // 必填；Astro Content Collection 名称
  },
  "homeObject": {
    "sectionKey": "photos", // 必填；关联栏目 key
    "asset": "/assets/camera-sticker.webp", // 必填；首页物件素材路径
    "desktopPosition": { "x": 0.2, "y": 0.58 }, // 必填；0–1 相对单页坐标
    "mobileOrder": 3, // 必填；1–8，窄屏阅读顺序
    "draggable": false // 默认值: false；仅非关键纸片可设 true
  }
}
```

**设计决策：** 内容条目只保存可迁移、可校验的事实；视觉坐标与内容解耦，以免新增文章要求改首页模板。`draft` 是唯一公开性开关，`status` 不得替代它。P0 故意不保存阅读历史、用户身份、评论、布局偏好或图像编辑参数。

## 第六章：技术架构

```text
+--------------------------------------------------------------------+
| 内容维护层：Obsidian / 编辑器                                      |
| 责任：编写 Markdown、管理 frontmatter、引用已处理的图片 URL        |
+----------------------------------+---------------------------------+
                                   |
+--------------------------------------------------------------------+
| 内容与构建层：Astro Content Collections + schema                   |
| 责任：加载八个集合、校验字段、过滤草稿、静态生成路由                |
+----------------------------------+---------------------------------+
                                   |
+--------------------------------------------------------------------+
| 页面层：Astro 页面 + 共享布局/列表组件                              |
| 责任：首页、栏目页、详情页、元数据、可预测 URL                      |
+----------------------------------+---------------------------------+
                                   |
+--------------------------------------------------------------------+
| 体验层：CSS 设计令牌 + 原生 journal.js                              |
| 责任：纸张、字体、开本、栏目过渡、可选拖动、减少动态效果             |
+----------------------------------+---------------------------------+
                                   |
+--------------------------------------------------------------------+
| 交付层：静态 dist/ + Git 推送 + Cloudflare Pages（待接入）           |
| 责任：不可变静态发布、缓存、回滚到 Git 历史                         |
+--------------------------------------------------------------------+
```

| 库名 / 技术 | 用途 | 为何优于替代方案 | 大致包体积 |
|---|---|---|---:|
| Astro 5 | 静态路由、组件、构建 | 内容站默认输出 HTML，运行时 JS 极少；适合 Markdown 归档。 | 未知 |
| Astro Content Collections + Zod schema | 内容加载与 frontmatter 校验 | 直接把内容错误拦在构建期，不引入 CMS 运行时。 | 随 Astro 提供，未知 |
| TypeScript | 栏目 key、元数据与构建期类型约束 | 防止八栏目 key/路由写错；已在项目使用。 | 构建期工具 |
| 原生 CSS + 原生 JavaScript | 纸张视觉、动效、拖动和过渡 | 当前交互有限，引入 React/动画库会增加复杂度和脚本成本。 | 无额外依赖 |
| fonttools `pyftsubset` | 中文字体子集化 | 保留指定字体风格，避免全量中文字体进入生产。 | 构建期工具 |

**最大架构风险：** 首页的自由绝对定位与全站可维护性产生冲突。应将“首页栏目入口的语义和路由”固定在数据/DOM 契约中，而把具体视觉位置、层次和素材留在样式层；不允许用图片热点或 JS 点击分支取代链接。

**可替换技术原则：** 推荐继续使用 Astro + Content Collections + 原生 CSS/JS。若项目已有更成熟的轻量动画工具，可用于开本与过渡；若已有图片 CDN，可直接替换 URL 来源。不可变的是静态可访问 URL、Markdown 可迁移内容、草稿不公开、无 JS 时能导航、减少动态效果时不阻塞访问。

## 第七章：交互细节

### 7.1 键盘快捷键

| 操作 | 快捷键 | 备注 |
|---|---|---|
| 顺序浏览首页栏目 | `Tab` / `Shift + Tab` | 焦点顺序必须与阅读顺序一致；不设计自定义方向键劫持。 |
| 进入当前栏目/条目 | `Enter` | 使用原生链接行为。 |
| 在首页重播开本 | `Tab` 到“重新打开”后 `Enter` | 辅助操作，不应抢占首个栏目入口焦点。 |
| 返回上一页 | 浏览器 `Alt + ←` / 浏览器后退 | 不重复造站内历史机制。 |

### 7.2 右键菜单与上下文菜单

P0 不自定义右键菜单，保留浏览器原生复制链接、在新标签打开、保存图片等能力。自定义菜单会降低浏览器预期且没有核心用户价值。

```text
内容图片（浏览器原生菜单）
+----------------------+
| 在新标签页打开图片    |
| 复制图片地址          |
| 保存图片              |
+----------------------+
```

### 7.3 空状态

| 页面 | 用户看到的内容 | CTA |
|---|---|---|
| 新栏目无条目 | “手账这一页暂时空白。”加一句栏目语境文案 | “返回手账首页” |
| 首页素材未齐 | 不展示破图占位；使用已定义的文字标签和纸张元素 | 浏览八个栏目 |
| 找不到详情 | 清楚说明该页不存在或尚未公开 | “返回手账首页”与有效栏目链接 |

### 7.4 错误状态

| 触发条件 | 用户可见的提示信息 | 恢复操作 |
|---|---|---|
| 不存在或草稿详情 URL | “这页手账还没有公开，或已经被翻到别处了。” | 返回首页或所属栏目 |
| 栏目无公开内容 | “手账这一页暂时空白。” | 返回首页浏览其他页 |
| 内容封面/正文图片加载失败 | alt 文本与正文仍可读；不显示全屏报错 | 刷新或稍后重试；创作者修正 URL |
| 外链失效 | 本站不拦截，外部标签页显示其原始错误 | 返回本站继续阅读 |
| 构建校验失败 | 仅维护者在终端看到具体字段/行错误 | 修正 Markdown 后重新 check/build |

### 7.5 加载状态

| 操作 | 指示器 | 不显示阈值 | 劣化处理 |
|---|---|---:|---|
| 首页开本 | 页面自身展开，不增加 spinner | `< 200ms` | 超过 900ms 强制显示已打开的静态状态 |
| 栏目跳转 | 简短栏目标题过渡层 | `< 150ms` | 超过 700ms 仍直接导航，避免假加载卡住 |
| 封面图片 | 原生 `loading="lazy"`（非首屏） | `< 200ms` | 显示 alt，卡片不折叠 |
| 本地构建/发布 | 终端命令输出 | 不适用 | 子集化、校验或构建失败时中止发布链路 |

## 第八章：导出与输出系统

本产品不在浏览器中导出文件；其“输出”是可访问、可分享、可版本化的静态网站及源 Markdown。维护者通过 Git 保留内容原件，访客通过稳定 URL 消费内容。

> 实拍物件（相机、酒、书、贴纸、印章等）从照片到透明资产的加工、命名与投产参数，见独立的《素材工程规范》`baozi-space-assets.md`：去背/描边不用生成模型、母版/投产双版、落影走 CSS，且投产版单张 ≤150KB、首屏合计对齐第十章 ≤900KB。

### 8.1 支持的输出格式

| 格式 | 使用场景 | 质量选项 | 备注 |
|---|---|---|---|
| Markdown / MDX | 创作者原始内容与长期备份 | UTF-8 文本、frontmatter schema | 生产内容的唯一事实来源 |
| 静态 HTML | 访客阅读、SEO、分享 | Astro 构建产物 | 不依赖登录或服务端渲染 |
| WebP / AVIF / PNG | 物件贴纸、照片与内容插图 | 按显示尺寸输出；透明物件保留 alpha | 图片处理在发布前完成 |
| WOFF2 | 中文字体子集 | 仅包含站点实际字形 | 新增中文文案后需重新子集化 |

### 8.2 输出文件结构

```text
dist/
├── index.html
├── blog/
│   ├── index.html
│   └── webpage-as-room/
│       └── index.html
├── thoughts/
│   └── index.html
├── photos/
│   └── index.html
├── drinks/
│   └── index.html
├── books/
│   └── index.html
├── music/
│   └── index.html
├── about/
│   └── index.html
├── ai-works/
│   └── index.html
├── fonts/
│   ├── lxgw-wenkai-subset.woff2
│   └── slide-xiaxing-subset.woff2
└── assets/
    └── camera-sticker.webp
```

### 8.3 批量处理流程

```text
多个 Markdown 条目
      |
      +-- 可并行 --> schema 解析 / 路由静态生成 / 非首屏图片优化
      |
      +-- 必须串行 --> 扫描全站中文字符 → 子集化字体 → 构建 dist → 提交推送
      |
      v
静态部署平台构建 → pages.dev 预览（接入后）→ baozi.space 正式域名（绑定后）
```

串行步骤不能颠倒：若先构建再生成字体子集，新写入的中文字符可能在生产中回退为缺字。

## 第九章：开发优先级

### P0 - 没有这个，产品根本无法使用。交付标准：功能可用，不需要完美。

- 首页可从合上/静态降级进入当前全部语义栏目入口（现阶段 Blog、Thoughts、Photos、About）。
- 八个栏目拥有列表和详情稳定路由；所有公开内容可日期倒序阅读。
- Markdown schema、草稿过滤和草稿详情 404 正确生效。
- 站点在桌面与 390px 窄屏可用；键盘可完成首页入口与内容导航。
- `prefers-reduced-motion` 时无长动画、无导航阻塞。
- `bun run check`、`bun run build` 可通过。

### P1 - 没有这个，用户第一次体验后不会回来。交付标准：功能完整，体验有连续性。

- 用真实首页物件、正式栏目文案、至少每栏目一条可读内容替换占位素材。
- 栏目页与详情页形成更明确的纸张/批注编辑语言，回首页路径清晰。
- 图片在内容卡、正文图、拍立得图与图注之间有一致且可访问的规则。
- Obsidian 模板、字体子集化和发布顺序形成可重复维护流程。
- Cloudflare Pages 完成首次部署与自定义域名绑定。

### P2 - 有了这个，用户会把产品推荐给别人。交付标准：稳定且有辨识度。

- 移动端拥有独立的连续手账叙事，而非桌面缩放版。
- 首页以可维护方式呈现“最近翻到这一页”或人工精选内容。
- 不同栏目形成克制但可辨的布局语言；照片/音乐/AI Works 能更贴合媒介。
- 关键页面通过真实键盘、减少动态效果、慢网图片失败等回归检查。

### P3 - 有了这个，一部分用户会付费或强烈倡导。交付标准：精致且有完整文档。

- 在不追踪个人身份的前提下，增加清晰的订阅/RSS 或更新通知方案。
- 设计可分享的阅读摘录/手账页图卡，保持内容版权和来源说明。
- 建立素材尺寸、图片版权来源、内容迁移和部署回滚的完整维护文档。

## 第十章：性能指标

| 指标名称 | 目标值 | 测量方法 | 劣化阈值 |
|---|---:|---|---:|
| 首页首次内容绘制（桌面） | `<= 1,500ms` | Chrome DevTools Performance，节流 Fast 3G、冷缓存 | `> 2,500ms` |
| 首页可交互时间（桌面） | `<= 2,000ms` | Lighthouse / DevTools，点击首个栏目链接到可响应 | `> 3,000ms` |
| 开本动画时长 | `450–900ms` | 浏览器 Performance 标记或 CSS/JS 时序 | `> 1,200ms` |
| 减少动态效果导航额外延迟 | `<= 100ms` | 启用系统减少动态效果后点击栏目并记录路由开始 | `> 250ms` |
| 首屏图片总传输体积 | `<= 900KB` | Network 面板冷缓存统计首页首屏图片/字体 | `> 1.5MB` |
| 中文字体总传输体积 | `<= 350KB` | Network 面板统计本地 WOFF2 | `> 600KB` |
| 首屏非关键图片懒加载覆盖率 | `100%` | 审查所有首屏以下 `<img>` 是否有 `loading="lazy"` | `< 90%` |
| 键盘到首个可激活栏目 | `<= 3` 次 Tab | 无鼠标从地址栏进入页面手动测试 | `> 5` 次 Tab |
| 首页栏目链接成功率 | `100%` | 自动/手动遍历八个 href，检查 200 或静态成功页 | `< 100%` |
| 草稿泄露率 | `0` | 创建 `draft: true` 样本，检查列表缺席且详情为 404 | `> 0` |
| 生产构建失败率 | `< 1%` 发布尝试 | 记录 `bun run check && bun run build` 成功次数 | `>= 5%` |

## 第十一章：开发者交接说明

### a) 实现顺序建议

你先守住内容闭环，而不是先追求更复杂的翻页：

1. 确认八个 collections、列表、详情和草稿过滤；这是所有页面可用的前提。
2. 确认首页当前全部入口都是可读的语义链接，JS 关闭时仍能路由；这锁住体验底线。
3. 在此基础上打磨桌面开本、栏目过渡、可拖动纸片与纸张视觉；这些都是增强层。
4. 再用真实素材替换物件，并单独设计移动端连续叙事。
5. 最后接通 Cloudflare Pages、域名和真实发布回归。

### b) 最可能导致返工的三个决策

1. **首页的视觉自由度如何与可访问性共存。**
   - 安全默认：先写阅读顺序正确的 HTML 链接，再以 CSS 定位成自由手账。
   - 改变方向信号：若 Tab 焦点跳跃、屏幕阅读器读不出栏目、窄屏发生遮挡，就减少叠放并重排 DOM/样式；不要补 JS 热点。

2. **移动端是否保留双页。**（2026-07-24 已决策）首页改为单页平铺；移动端 ≥660px 等比缩放整张纸、<660px 纵向重排。双页跨页基线已过时。

3. **内容模型何时分化。**
   - 安全默认：所有栏目共用现有 entry schema，差异先写入正文和标签。
   - 改变方向信号：同一栏目连续至少 5 篇需要同一个结构化字段（例如 Drinks 的 ABV、Photos 的相机），再新增可选字段并更新模板；不要为一篇特例污染全站 schema。

### c) 哪里要严格，哪里可以灵活

| 章节/主题 | 级别 | 说明 |
|---|---|---|
| 第一章定位与边界 | 约束 | 不做账户、评论或重型 3D；它们改变产品类型。 |
| 第二章八栏目 IA 与稳定 URL | 约束 | 这是内容长期链接与首页语义的骨架。 |
| 第三章内容可见性、草稿过滤、失败路径 | 约束 | 草稿泄露和死链会直接破坏可信度。 |
| 第四章纸张、物件与批注表达 | 建议 | 需随真实素材迭代，但不能遮挡导航或正文。 |
| 第五章核心字段 | 约束 | 保持可迁移、可校验；扩展字段必须有重复需求证据。 |
| 第六章技术选型 | 建议 | 可调整工具，但静态、可访问、低 JS、可维护是不变量。 |
| 第七章微交互与空状态文案 | 建议 | 可发挥设计品位，仍需满足响应/降级阈值。 |
| 第八章输出和发布顺序 | 约束 | 字体子集化必须在构建/发布前完成。 |

### d) 已知的未知项

- 此处未解决：真实的首页八个物件、手写批注、正式文案与版权/来源说明尚未齐备；不能将临时素材当作正式资产。
- 此处未解决：Cloudflare Pages 是否已连接 Git 仓库，`baozi.space` 的 DNS 迁移/绑定尚未完成；先以本地/预览环境验证。
- 此处未解决：PicGo 最终图床与图片保留策略；所有远程图片都应有可替换源地址与 alt 文本。
- 此处未解决：旧 Gridea 内容的数量、结构和迁移优先级；迁移前先抽样映射 frontmatter，避免一次性导入低质量旧条目。

### e) 验收剧本

验收剧本 1：首页桌面。在 1440×900 Chrome 冷缓存打开 `/`，等待开本；首页当前入口为 Blog、Thoughts、Photos、About 四个，依次点击应进入各自 `/section`；装饰物件不可点击；直接访问 `/drinks`、`/books`、`/music`、`/ai-works` 应仍正常打开。用地址栏路径与截图验证。
验收剧本 2：键盘与降级。在 1440×900 启用系统“减少动态效果”，刷新首页后用 Tab/Enter 进入任意两个栏目；应不依赖拖动、不等待长动画。用录屏或 Performance 时间线验证导航额外延迟不超过 100ms。
验收剧本 3：草稿保密。新建任一栏目测试 Markdown，填写 `draft: true`；运行 `bun run build` 后，它不得出现在栏目列表，直接访问其预期详情 URL 应为 404。再将其改为 `false` 重新构建，列表与详情均应出现。用构建输出和浏览器页面验证。
验收剧本 4：移动阅读。在 390×844 视口打开首页并滚动至底部；首页全部当前入口都应有可读标签、至少 44px 的可点击区域，且纵向滑动不会误触栏目。用截图和手动触控模拟验证。
验收剧本 5：内容发布回归。在新 Markdown 中加入中文标题与正文，运行 `bun run fonts:subset && bun run check && bun run build`；应成功生成本地 WOFF2 和 `dist/`，Network 面板中中文字体可加载。用命令输出、构建产物和 `document.fonts.check()` 验证。
