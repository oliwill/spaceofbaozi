# baozi.space PRD

**产品名：** 中华一番包子铺

**版本：** V2.0 Draft · Phase 1

**更新时间：** 2026-07-30

**文档状态：** 产品结构已定义；个人内容与关键编辑决策等待用户输入

**生产基线：** Astro 静态站 `E:\baozi`

**唯一视觉论题：** `binder-archive（活页档案）`

**Reader Test：** 2026-07-30 通过；0 Blocking、0 Should fix

> 本 PRD 是 Phase 1 的产品事实来源。它定义页面目标、信息层级、内容责任、功能边界和验收条件，不替代 Phase 2 的正式设计规范，也不把原型中的临时文案当作用户事实。

## 文档地图

- Phase 0 基线：`docs/project/phase-0-baseline.md`
- 用户内容输入：`docs/project/phase-1-content-input.md`
- 现有内容批准清单：`docs/project/content-approval-register.md`
- 已确认决策：`docs/project/decision-log.md`
- 现有素材管线：`baozi-space-assets.md`
- V1 设计规格：`baozi-space-spec.md`（历史参考，已非 V2 设计事实来源）
- V2 视觉原型：`E:\baozi-rivet-artifacts\binder-archive`

## 标记约定

- **Confirmed：** 用户已经确认，后续工作必须遵守。
- **Requirement：** 为实现产品目标必须满足。
- **User Input：** 个人事实、正式文案或素材，必须由用户提供或明确批准。
- **Open：** 尚未决定；Open Questions 表中的“最迟决策阶段”说明何时必须关闭。只有标为 Phase 1 的问题会阻塞本阶段结束。
- **Phase 2：** 留给设计规范与 Style Recipe，不在本轮决定具体视觉参数。

---

# 0. 执行摘要

## 0.1 产品一句话

`baozi.space` 是包子的个人档案与数字手账：访客先从桌面上的透明活页本和卡片认识“包子是谁、这里记录什么”，再打开本子，通过周记式主页阅读最新内容，进入七个长期内容栏目或独立 About。

## 0.2 用户应获得的结果

访客不需要先理解复杂交互，即可：

1. 在首屏理解网站主人和网站用途；
2. 查看或抽取卡片，获得更完整的信息；
3. 打开手账并看到最新/精选内容；
4. 浏览全部七个内容栏目，而不是只看到少数装饰入口；
5. 直接收藏、分享和返回任何栏目或内容 URL；
6. 在桌面、手机、键盘和减少动态效果模式下完成同样的核心任务。

## 0.3 当前项目阶段

| 项目 | 状态 |
|---|---|
| 技术骨架 | 已存在，27 个静态页面可构建 |
| 当前生产栏目与路由 | 已存在；V2 目标分组与迁移已确认，尚未实施 |
| V2 视觉方向 | 已确认 `binder-archive` |
| V2 PRD / IA | 本文定义 |
| 用户正式内容 | 尚未提供完整 |
| V2 设计规范 | Phase 2 |
| 高保真设计确认 | Phase 4 |
| 正式页面开发 | 尚未开始 |
| Cloudflare Pages | 尚未接入 |

## 0.4 核心边界

- 启动页是信息页，不是纯装饰 splash。
- 活页本、抽卡和翻页是空间隐喻，不得替代语义链接和可读文字。
- V2 不引入重型 3D 物理引擎。
- 用户个人事实、经历、介绍和正式栏目文案不得由 AI 虚构。
- 正式设计确认前不迁移原型代码到生产。

---

# 1. 产品定义

## 1.1 要解决的问题

当前个人内容分散在旧博客、社交平台、图片和本地记录中。传统博客能阅读但难以体现个人审美；自由拼贴能体现个性但容易让访客不知道从哪里开始。

V2 需要同时解决：

1. **身份不清：** 首次访问者不能快速理解包子是谁。
2. **用途不清：** 访客不知道网站包含什么、为何值得继续浏览。
3. **内容入口不稳定：** 视觉物件和栏目之间缺少统一规则。
4. **首页秩序不足：** 装饰、问候、栏目与更新争夺注意力。
5. **内容长期维护：** 新内容必须继续通过 Markdown、Git 和稳定 URL 生长。

## 1.2 产品目标

### G1 · 五秒理解

启动页默认状态无需抽完所有卡片，也能让访客理解：

- 这是包子的个人网站；
- 它是一份持续生长的个人档案；
- 包子的简短身份和建立网站的原因；
- 如何进入主页。

七个内容栏目的完整目录由主页负责，不在启动页重复呈现。

### G2 · 一分钟找到内容

访客进入主页后，可以在一分钟内：

- 打开首要内容；
- 找到最近更新；
- 找到任意一个栏目；
- 返回主页或上一级。

### G3 · 视觉与阅读属于同一个系统

启动卡、主页周记、栏目归档和详情页必须共用：

- 稳定的编号和栏目命名；
- 一致的字体角色；
- 可预测的内容层级；
- 纸张/档案语境；
- 明确的返回路径。

具体字体、颜色、纸张和动效参数由 Phase 2 决定。

### G4 · 用户声音可信

所有正式身份、介绍、栏目说明、内容正文和图片来源都可追溯到用户输入。Codex 可以整理和润色，但最终版本必须由用户确认。

### G5 · 可持续维护

新增内容仍以 Markdown / MDX 为主，不要求进入页面代码，不依赖不可迁移的 CMS。

## 1.3 非目标

V2 当前不做：

- 用户账号、评论、私信、点赞或社区关系；
- 在线内容编辑器；
- 自由拖拽并持久化的画布；
- 完整物理模拟的 3D 活页本或翻书引擎；
- 依赖 IP 的所在地欢迎语；
- 在访问路径中调用生成式 AI；
- 电商、餐饮菜单或付费结算；
- 为少量内容预先建立复杂筛选系统；
- 用生成图片承载重要长文或唯一导航文字。

## 1.4 产品原则

1. **内容先于装饰。**
2. **物理隐喻解释结构，不制造障碍。**
3. **一个入口只承担一种主要意图。**
4. **所有视觉导航都有语义和文字等价物。**
5. **真实内容先进入设计，再批量生产素材。**
6. **桌面和手机共享信息层级，但允许不同构图。**
7. **动效可以消失，内容不能消失。**
8. **占位内容不等于用户内容。**

## 1.5 受众需求

本项目不以虚构人物画像作为决策依据，按真实访问意图划分：

| 访问意图 | 需要快速获得 |
|---|---|
| 第一次认识包子 | 身份、关注主题、网站用途、推荐起点 |
| 从外链进入某篇内容 | 清晰正文、来源/日期、所属栏目、返回路径 |
| 回访者 | 最近更新、栏目索引、稳定 URL |
| 未来合作或联系 | 可信个人介绍、公开边界、用户批准的联系方式 |
| 包子本人 | 可维护的 Markdown、草稿保护、图片与发布流程 |

---

# 2. 范围、责任与内容所有权

## 2.1 已锁定与可调整

| 已锁定 | Phase 1 待确认 | Phase 2 可设计 |
|---|---|---|
| 个人档案 / 数字手账定位 | 七个栏目的正式说明与收录边界 | 桌面和活页本视觉 |
| binder-archive 方向；启动页含网站简介、About Me 与装饰图片卡 | 装饰图片素材清单 | 装饰图片卡在 3–5 张内的最终数量、尺寸、材质和层次 |
| 七个内容栏目 + 独立 About；目标路由与迁移 | 七栏首批真实内容与 About 长版 | 周记网格、色彩和字体 |
| Astro 静态内容架构；七栏非空生产门禁 | 页面固定文案与内容样本 | 卡片抽取和翻页细节 |
| 内容由用户提供 | About / 联系公开边界 | 栏目页与详情页视觉 |
| 先设计确认再开发 | SEO 与分享信息 | 图片 Style Recipes |

## 2.2 责任矩阵

| 工作 | 包子 | Codex |
|---|---|---|
| 个人事实、经历、身份 | 提供并批准 | 不虚构；整理结构 |
| 网站目的与语气 | 提供原始表达并批准 | 提供编辑建议 |
| 栏目收录边界 | 最终决定 | 提示重叠和缺口 |
| 正式文章、照片、作品 | 提供并确认可公开 | 检查字段、排版和 alt |
| 页面需要哪些信息 | 共同确认 | 起草产品结构 |
| 交互与设计方案 | 审批 | 产出与迭代 |
| 工程实现与 QA | 验收 | 实现、测试和记录 |
| 发布与域名 | 批准生产发布 | 准备配置和回滚方案 |

## 2.3 内容状态

| 状态 | 含义 | 可用于设计 | 可用于生产 |
|---|---|---:|---:|
| Placeholder | 系统占位或结构示例 | 只可测试结构 | 否 |
| Raw | 用户原始信息，尚未整理 | 是 | 否 |
| Draft | 已整理，等待用户确认 | 是 | 否 |
| Approved | 用户明确确认，并记录在内容批准清单 | 是 | 可以进入发布候选 |
| Published | Approved 内容已通过 schema、QA 并公开 | 是 | 是 |

这些是**编辑工作流状态**，不等同于当前代码 schema。Phase 5 实现前，以 `docs/project/content-approval-register.md` 记录批准状态；任何未标为 Approved 的现有内容都不得因 `draft: false` 自动获得发布资格。

---

# 3. 信息架构

## 3.1 V2 目标路由树

以下目标路由与永久跳转已经确认；本阶段只更新产品文档，尚未改动生产路由。

```text
baozi.space
│
├── /                         首页路由
│   ├── 启动状态              桌面 + 透明活页本 + 可抽卡
│   └── 主页状态              周记式内容页
│
├── /blog                     文章归档
│   └── /blog/<slug>          文章详情
├── /photos                   摄影归档
│   └── /photos/<slug>        摄影详情
├── /projects                 项目归档
│   └── /projects/<slug>      项目详情
├── /drinks                   酒记录归档
│   └── /drinks/<slug>        酒记录详情
├── /books                    阅读概览
│   ├── /books/archive        完整阅读档案
│   └── /books/<slug>         阅读详情
├── /music                    音乐概览
│   ├── /music/archive        完整音乐档案
│   └── /music/<slug>         音乐详情
├── /movies                   观影概览
│   ├── /movies/archive       完整观影档案
│   └── /movies/<slug>        观影详情
├── /about                    完整个人档案
├── /thoughts/*               旧链接 → 对应 /blog/*
├── /ai-works/*               旧链接 → 对应 /projects/*
├── /about/me                 旧链接 → /about
└── 404                       无效或未公开路径
```

## 3.2 页面层级

```text
Level 0  启动页：网站简介 / About Me / 打开本子
   ↓
Level 1  主页：首要内容 / 七个内容栏目目录 / 最近更新 / About 摘要
   ↓
Level 2  栏目归档：栏目说明 / 条目列表 / 空状态
   ↓
Level 3  内容详情：完整内容 / 上下文 / 返回路径
```

## 3.3 导航契约

- V2 使用七个内容栏目（Blog、Photos、Project、Drinks、Books、Music、Movies）和独立 About。
- 现有 `/blog`、`/photos`、`/drinks`、`/books`、`/music`、`/about` 保持。
- 新增 `/projects`、`/movies`。
- 旧 `/thoughts/*`、`/ai-works/*`、`/about/me` 不直接失效；迁移后使用逐条永久跳转保留外链、收藏和分享。
- 启动页只呈现网站简介与 About Me；网站简介卡只阅读，About Me 卡使用真实链接进入 `/about`。
- Blog、Photos、Project、Drinks、Books、Music、Movies 全部只在主页目录中承担栏目导航，不在启动页重复出现。
- V2 生产发布时，七个内容栏目必须各有至少一条公开且已批准的真实内容，并全部出现在主页稳定文字索引中。
- 主栏目与次栏目使用相同发布门禁；“主/次”只影响首页层级，不影响是否需要真实内容和用户批准。
- 如果预览、迁移或异常状态出现零条有效内容的栏目，保留 URL，在栏目页和主页目录标记“正在整理”，并设置 `noindex`；该回退不是允许空栏目进入生产的替代方案。
- 栏目页必须返回主页。
- 详情页必须返回所属栏目，并能到达主页。
- 任何转场都不能改变 URL 语义。
- 刷新、浏览器后退、在新标签打开和复制链接保持原生行为。

## 3.4 首页的两个状态

启动页和主页位于同一路由 `/`，但属于两个明确状态：

### State A · Launch

- 负责身份、目的和进入方式；
- 可以探索卡片；
- 不能遮蔽直接内容访问；
- 减少动态效果或 JavaScript 不可用时必须有可用降级。

### State B · Home

- 负责内容发现和栏目导航；
- 不依赖 Launch 状态才能被搜索引擎或辅助技术理解；
- 从栏目或详情返回 `/` 时直接显示 Home；每个浏览会话只自动显示一次 Launch，主页可通过“合上本子”主动重看。

---

# 4. 页面需求

## 4.1 启动页 `/` · Launch

### 页面任务

1. 说明“我是谁”；
2. 说明“这个网站是做什么的”；
3. 提供完整 About 的明确入口；
4. 提供明确、稳定的“打开本子”动作。

### 内容单元

| 单元 | 内容来源 | 状态 |
|---|---|---|
| 网站名 | User Input | `中华一番包子铺`，已确认 |
| 网站简介卡 | User Input | 网站用途已确认；卡片标题与最终长度待内容确认 |
| About Me 卡 | User Input | 公开署名与一句话身份已确认；链接到 `/about` |
| 卡片结构 | User Input | 方案 B 已确认：2 张信息卡 + 3 张装饰图片卡为 Demo 基线；Phase 2 可在 3–5 张内调整 |
| 栏目目录与最近更新 | Home 内容系统 | 不在 Launch 显示 |
| 全站最近更新时间 | 内容系统 | 不在 Launch 显示；可在 Home 使用 |
| 活页本、桌面、卡面素材 | 用户提供原始素材/参考并最终批准；Codex 可按 Phase 2 Recipe 制作 | 未完成 |
| 打开按钮文案 | User Input | `打开本子`，已确认 |

### 功能要求

- **FR-LAUNCH-01：** 默认状态必须显露网站名、网站简介线索、About Me 身份线索和主动作。
- **FR-LAUNCH-02：** 卡片堆叠时，每张可交互卡都有可读标题条和最小触达区域。
- **FR-LAUNCH-03：** 同一时间最多一张卡处于完整阅读位。
- **FR-LAUNCH-04：** 点击/Enter 抽取或收回卡片；Escape 和空白区域可收回。
- **FR-LAUNCH-05：** 卡片正文使用 HTML 文本；图片不能是唯一信息来源。
- **FR-LAUNCH-06：** About Me 卡使用真实 `<a href="/about">`；不能用抽卡脚本模拟链接。
- **FR-LAUNCH-07：** “打开本子”不依赖抽卡完成。
- **FR-LAUNCH-08：** 不能要求用户自由拖动物件才能继续。
- **FR-LAUNCH-09：** 每个浏览会话首次进入 `/` 时显示 Launch；会话内再次进入 `/` 时直接显示 Home。
- **FR-LAUNCH-10：** Launch 状态使用会话级本地保存，关闭标签页后失效；主页提供“合上本子”入口主动重看。
- **FR-LAUNCH-11：** 从栏目或详情返回 `/` 时直接进入 Home，不重复播放 Launch。
- **FR-LAUNCH-12：** 信息卡固定为网站简介与 About Me 两张；不得加入最近更新卡或栏目目录卡。
- **FR-LAUNCH-13：** 网站简介卡只阅读，About Me 卡进入 `/about`；装饰图片卡可以抽出和放大查看，但不承担栏目导航或动态信息。
- **FR-LAUNCH-14：** Launch 不显示七栏目目录、最近更新或全站更新时间；这些信息由 Home 负责。
- **FR-LAUNCH-15：** 采用方案 B，Demo 以 3 张装饰图片卡为基线；Phase 2 可根据桌面与手机构图在 3–5 张之间确定最终可见数量，但不能全部取消。

### 不采用

- 任意自由拖拽和碰撞；
- 必须按固定顺序抽完卡片；
- 只有“点击任意处”而没有明确操作；
- 卡片彼此完全遮挡；
- 在启动页重复主页的七栏目目录、最近更新或全站更新时间；
- 用一张扁平大图伪装全部可交互卡片。

## 4.2 主页 `/` · Home

### 页面任务

1. 提供一个明确的内容主锚点；
2. 显示全部七个内容栏目；
3. 显示最近更新；
4. 用短 About 信息补充人格；
5. 建立“周记页”的阅读节奏，而不是重新堆一张自由拼贴。

### 推荐内容顺序

以下是信息层级，不是最终坐标：

1. 页眉 / 当前版面标题或更新时间；
2. 首要内容；
3. 七个内容栏目索引；
4. 最近更新；
5. About 摘要；
6. 由内容驱动的少量插画或贴纸。

### 内容单元

| 单元 | 内容来源 | 状态 |
|---|---|---|
| 主页版面标题语气 | User Input | 未提供 |
| 首要内容 | 内容系统 + 用户编辑规则 | 人工精选 + 主栏目最新回退，已确认 |
| 首要内容标题/摘要/日期 | Content Collection | 已支持 |
| 七个内容栏目名称和说明 | User Input 批准后的 metadata | 分组已确认；说明与边界待确认 |
| 最近更新 | Content Collection | 全栏目 3 条并排除首要精选，已确认 |
| About 摘要 | User Input | Work / Life 短介绍与 `/about` 入口已确认 |
| 固定插画、照片和贴纸 | User Input / Asset Recipe | Phase 2 |

### 功能要求

- **FR-HOME-01：** 首要内容在视觉和 DOM 阅读顺序中均优先于装饰。
- **FR-HOME-02：** Blog、Photos、Project、Drinks、Books、Music、Movies 必须全部出现在稳定索引中。
- **FR-HOME-03：** 栏目入口共用编号、名称、中文说明和行动线索。
- **FR-HOME-04：** 最近更新由公开内容计算，不读取草稿。
- **FR-HOME-05：** 首页图片必须绑定到对应内容或明确的档案说明。
- **FR-HOME-06：** 手机端使用同一语义顺序单列呈现，不缩成不可读的桌面稿。
- **FR-HOME-07：** 生产发布门禁必须保证至少有一个主栏目内容可用于首要内容回退；预览异常时可隐藏首要内容区，但不得自动生成虚假文章。
- **FR-HOME-08：** About 摘要必须保留 Work / Life 两层身份，不把职业经历写成唯一身份；“当前学习”属于带更新时间的可变信息。
- **FR-HOME-09：** 主页直接复用已批准的 Work / Life 短介绍，并以“继续认识包子”链接到 `/about`；具体行业经历和当前学习不在主页重复展开。

“周记网格”只描述横线、日期轨、重复条目和图文绑定的**视觉阅读节奏**，不意味着网站必须每周更新，也不自动生成固定刊期。

## 4.3 栏目归档页 `/<section>`

### 页面任务

- 告诉访客该栏目收录什么；
- 默认按内容原始发布日期倒序；
- 帮助进入条目；
- 生产发布时栏目至少有一条真实内容；预览或迁移异常为空时仍有明确回退状态和返回路径。

### 每个栏目需要

| 内容 | 来源 |
|---|---|
| 英文名 / 中文名 | User Input |
| 一句话栏目说明 | User Input |
| 收录范围 / 不收录范围 | User Input |
| 代表图片或图形（可选） | User Input / Phase 2 |
| 异常空状态文案 | 系统固定为“正在整理” |
| 条目标题、摘要、日期、标签、封面 | Content Collection |

### 功能要求

- **FR-ARCHIVE-01：** 默认只显示 `draft: false && approved: true`。
- **FR-ARCHIVE-02：** 条目标题和摘要不依赖封面图片。
- **FR-ARCHIVE-03：** 每个条目链接有可预测 URL。
- **FR-ARCHIVE-04：** 无内容时不显示空卡片墙。
- **FR-ARCHIVE-05：** 栏目页有返回主页和全站导航路径。
- **FR-ARCHIVE-06：** Photos 等栏目是否采用专属布局，要等真实内容样本验证，不能提前为占位内容增加复杂组件。
- **FR-ARCHIVE-07：** V2 首版不支持手工编辑排序；若未来需要策展，必须新增明确字段和验收规则。
- **FR-ARCHIVE-08：** V2 生产发布前，七个内容栏目必须分别满足 `publishableCount >= 1`；任一栏目为零即阻塞发布。
- **FR-ARCHIVE-09：** 零条有效内容只作为预览、迁移或异常回退：保留 URL，显示“正在整理”，设置 `noindex`，且不参与精选、最近更新或全站更新时间。
- **FR-ARCHIVE-10：** “正在整理”和 `noindex` 必须由 `draft: false && approved: true` 的有效内容数量派生；第一条有效内容发布后自动取消，不使用需要人工同步的独立开关。
- **FR-ARCHIVE-11：** 主栏目与次栏目应用相同的有效内容判定、用户批准与生产发布门禁。

## 4.4 内容详情页 `/<section>/<slug>`

### 页面任务

- 提供完整内容；
- 保留所属栏目、日期、标签与来源；
- 支持外链、图片和长文；
- 提供清晰返回路径。

### 内容要求

| 字段 | 责任 |
|---|---|
| 标题 | User Input |
| 一句话摘要 | User Input |
| 日期 | User Input |
| 标签 | User Input |
| 正文 / 项目说明 | User Input |
| 图片 / 封面 | User Input |
| 图片 alt | User Input；Codex 可辅助起草 |
| 外部来源 / 链接 | User Input |
| 草稿 / 精选状态 | User Input |
| 页面模板和元数据输出 | 系统 |

### 功能要求

- **FR-DETAIL-01：** 正文在无图片和无 JavaScript 时仍可完整阅读。
- **FR-DETAIL-02：** 草稿详情 URL 不生成。
- **FR-DETAIL-03：** 远程图片失败不隐藏标题和正文。
- **FR-DETAIL-04：** 外链清楚标示，不能让用户误以为仍在本站。
- **FR-DETAIL-05：** 返回所属栏目，并提供主页路径。
- **FR-DETAIL-06：** 重要文字不得烤进图片。

## 4.5 About

About 是用户身份信息的正式来源，启动页和主页只引用其短版。

### User Input

- 短版介绍；
- 长版介绍；
- 当前关注 / 当前在做什么；
- 愿意公开的身份、地点或职业；
- 不愿公开的边界；
- 联系方式与社交链接；
- 代表照片或插画；
- 是否接受合作、委托或工作联系。

### 功能要求

- **FR-ABOUT-01：** 未经用户确认，不显示推断的职业、所在地或经历。
- **FR-ABOUT-02：** 联系方式按用户公开边界展示。
- **FR-ABOUT-03：** 启动页、主页和 About 的事实一致，但长度可以不同。
- **FR-ABOUT-04：** `/about` 直接作为完整 About 页面；旧 `/about/me` 永久跳转至 `/about`。

## 4.6 404、SEO 与分享

### 页面要求

- 404 使用统一、泛化的不可访问说明，不向访客区分某个 slug 是不存在、草稿还是已移动；
- 提供返回主页和有效栏目入口；
- 全站拥有用户批准的 title、description、favicon 和默认 OG 图；
- 内容页元数据来自条目；
- 分享图不暴露未公开个人信息。
- 零条有效内容的栏目回退页设置 `noindex`；有第一条公开且已批准内容后自动恢复可索引。

### User Input

- 全站浏览器标题；
- meta description；
- 默认分享图或其制作授权；
- favicon / 品牌标志；
- 404 文案语气；
- 图片署名与版权规则。

---

# 5. V2 目标栏目产品定义

内容分组、`/projects`、`/movies` 和旧 URL 跳转已经确认；正式说明与收录边界仍等待用户填写。当前生产仓库仍保留 Phase 0 记录的旧栏目，正式开发前不改动。

七个内容栏目均为 V2 生产首发范围，不采用“先上线空栏目、以后再补内容”的分批公开方式。每个栏目必须在生产发布前至少有一条真实、公开且已批准的内容；About 也必须使用用户批准的正式内容。主栏目与次栏目使用相同门禁。

| 编号 | 目标路由 | 名称 | 层级 | 产品角色 | Phase 1 User Input |
|---:|---|---|---|---|---|
| 01 | `/blog` | Blog | 主栏目 | 包子撰写的文章，包含长文与短文；完整正文以 Blog 为唯一正式来源，并承接经批准迁移的原 Thoughts | 《料亭菜单 NO.5——将心注入》已标记为 `Phase 2 Design-ready / Publication pending`；发布状态仍为 Edit，门禁计数为 0，Phase 5 修订并最终批准后才可发布 |
| 02 | `/photos` | Photos | 主栏目 | “用相册和摄影系列，记录我看见并想留下的生活。”只收录包子原创摄影；以相册 / 系列为单位，简介必填、逐图 caption 可选；应用既定人物隐私门禁 | 首发相册由包子完整策展主题、排版与顺序；方案和真实素材待交付，门禁计数为 0 |
| 03 | `/projects` | Project | 主栏目 | “记录我参与、完成和持续试验的项目。”同时收录可公开的职业与个人项目；核心字段固定，详情章节按需展开 | 「中华一番包子铺」已标记为 `Phase 2 Design-ready / Publication pending`；精简详情、核心字段与图片方案已确认，真实截图、正式内容文件和最终批准待完成，门禁计数为 0 |
| 04 | `/drinks` | Drinks | 次栏目 | “关于酒款、地点与味道的个人记录。”每个唯一酒款只保留第一次饮用记录；使用文字感受与可选“愿意再喝”标记，不设数字 / 星级评分 | 首条真实记录由包子一次性交付；暂不拆解，门禁计数为 0 |
| 05 | `/books` | Books | 次栏目 / 书影音 | “记录读过和正在读的书，以及它们留下的想法。”只收录书籍；核心字段必填、扩展书目选填；使用独立 `currentlyReading` 展示 Top 3；不设评分 | 首条真实记录由包子一次性交付；暂不拆解，门禁计数为 0 |
| 06 | `/music` | Music | 次栏目 / 书影音 | “记录最近在听，以及想长期留下的音乐。”只收录专辑、EP 与歌单；核心字段必填、扩展资料选填；“最近在听”自动取 Top 5；不设评分 | KOKIA《Songbird》保持 `Phase 1 Input pending`；暂时没有个人简短感受且不得生成代写内容，门禁计数为 0 |
| 07 | `/movies` | Movies | 次栏目 / 书影音 | “记录看过的电影与剧集，以及它们留下的感受。”电影与剧集共用统一主记录，以必填 `kind: movie | series` 区分；剧集核心字段为 `seasonCount` / `episodeCount`；每次完整看完写入作品级事件，重看新增事件；热力图只统计首次完成；累计使用互斥的“看过 / 在看”；最近列表合并两种状态并按最近活动排序；归档支持类型 / 状态 / 年份 / 标签筛选；不设评分 | 首发样本《火花》(2016) 已进入 `Phase 2 Design-ready / Publication pending`；门禁计数为 0 |
| 08 | `/about` | About | 独立页面 | Work / Life 身份、当前关注和公开边界 | 长版、联系信息、旧 URL 处理 |

**全站写作称谓：**

- 包子署名的公开文章、About、栏目说明及其他直接面向访客的作者文案，如需使用第二人称，统一写作「妳」，不自动替换为「你」。
- 第三方直接引文保持来源原貌，不因全站称谓规则改写。
- 导航、按钮和系统提示优先避免不必要的第二人称；确需使用时同样写「妳」。

迁移规则：原 Thoughts 内容并入 Blog，原 AI Works 内容并入 Project；所有旧详情页必须逐条建立永久跳转，不能只重定向栏目首页。

正式定义填写位置：`docs/project/phase-1-content-input.md`。

---

# 6. 内容模型与编辑规则

## 6.1 当前 schema

```yaml
title: string
description: string
date: date
tags: string[]
cover: string?
draft: boolean
status: string?
link: url?
featured: boolean
```

### V2 必须新增的通用门禁字段

Phase 5 实现前，所有 collection 增加：

```yaml
approved: boolean      # 默认 false；用户已批准进入生产候选
updated: date?         # 可选；内容发生实质更新的日期
coverAlt: string?      # 有 cover 时必填
coverCredit: string?   # 非本人封面或需要署名时必填
```

- `status` 只描述内容语义状态，不再控制公开与否；
- 可生成详情与进入列表的唯一条件是 `draft: false && approved: true`；
- 新模板默认 `draft: true`、`approved: false`；
- 内容批准清单中的 `Approve` 必须映射为 frontmatter `approved: true`；
- `Edit`、`Draft`、`Retire`、`Pending` 均保持 `approved: false`；
- 本轮只定义要求，不修改 schema 或现有内容。

## 6.2 发布要求

V2 公开内容至少具备：

- 非空标题；
- 一句话摘要；
- 有效日期；
- 正文或作品说明；
- 明确公开状态；
- 图片 alt；
- 非本人素材的来源或许可说明；
- 用户批准。

Photos 补充要求：

- 每本相册的主题、内部排版和图片顺序由包子确定；系统不得自动重排，响应式转换如需改变构图必须再次确认；
- 相册级简介必填；
- 单张照片的可见说明（caption）可选；
- 有内容意义的照片必须提供 alt，caption 不能替代 alt；
- 纯装饰图片才允许使用空 alt；
- 家人照片不得发布；
- 除包子本人外，其他可识别人物必须确认已获得明确同意；无法确认时需匿名化处理或不发布；
- 不在公开仓库保存包含个人身份信息的同意材料，只记录内容负责人在发布审批中的确认。

Project 补充要求：

- 职业项目只能使用已经公开、可验证的信息；
- 可以描述包子自己的职责、非保密方法与反思；
- 不公开内部数据、客户信息、内部截图、内部文件或无法确认授权状态的材料；
- 无法确定是否可公开时默认不发布；
- 每条职业项目在内容批准时单独确认保密门禁；
- 每条 Project 必须提供标题、类型、时间、状态、角色和一句话简介；
- Project 的 `status` 使用非空自由文本，不设枚举词表，只负责读者说明，不参与发布、排序或筛选逻辑；
- 背景、过程、成果 / 收获、图片、外部链接和相关文章为可选章节，不适用时不得渲染空模块。

Drinks 补充要求：

- 每条记录必须提供名称、类型、饮用日期、地点和简短感受；
- 品牌 / 酒厂、产地、酒精度、年份、照片、价格、搭配、外部链接和相关文章为选填；
- 选填字段不适用或未提供时直接省略，不生成空标签、假资料或占位内容；
- 每个唯一酒款只保留第一次饮用记录，之后再次饮用不新增条目或嵌套事件；
- Drinks 的数量表示记录过的唯一酒款数量，不表示实际饮酒次数；
- 不设置数字或星级评分；简短感受必填，“愿意再喝”为可选布尔标记；
- “愿意再喝”不换算分数，也不用于生成自动排行。

Books 补充要求：

- 每条记录必须提供书名、作者、载体、自由文本状态、记录日期和简短感受；
- 原名、译者、出版社、出版年份、开始 / 读完日期、封面、ISBN、来源链接和相关文章为选填；
- 选填字段不适用或未提供时直接省略，不生成空标签、假书目或占位内容；
- `currentlyReading` 为独立布尔标记，默认 `false`，不取代读者可见的自由文本状态；
- 不设置数字或星级评分，使用必填简短感受与人工推荐区表达判断；
- 不生成评分排行或算法推荐。

Music 补充要求：

- 每条记录必须提供名称、类型（专辑 / EP / 歌单）、艺人或创建者、记录日期和简短感受；
- 原名、发行年份、流派、厂牌、封面、最喜欢的曲目、平台链接和相关文章为选填；
- 选填字段不适用或未提供时直接省略，不生成空标签、假资料或占位内容；
- 不设置数字或星级评分，使用必填简短感受与人工推荐区表达判断；
- 不生成评分排行或算法推荐。

Movies 补充要求（电影 + 剧集，D-092 / D-095；字段与事件细节待确认）：

- 同一栏目收录电影与剧集；综艺和网络视频继续排除；
- 对外名称确认为 `Movies`、路由保留 `/movies`；正式说明确认为“记录看过的电影与剧集，以及它们留下的感受。”；
- 电影与剧集共用一套主记录模型，每个作品只建立一条唯一主记录；必填 `kind: movie | series` 负责类型区分，列表、归档和详情按 `kind` 显示并校验类型专用字段；
- 共同字段仍待确认；`kind: series` 的剧集核心字段确定为 `seasonCount` / `episodeCount`，单集时长、播出平台、主演、编剧、原名等按需选填；不得把剧集强行套用电影字段，也不维护两套重复 schema；
- 不记录逐季 / 逐集子记录、单集状态、单集感受或追剧进度；每次完整看完写入一条作品级观看事件，重看新增事件；剧集只在整部完成时记录；
- 热力图只统计每个作品的首次完整看完日期，每个作品贡献 1 个计数；重看事件保留但不进入热力图；累计统计使用互斥的“看过 / 在看”：首次完成后转为“看过”，重看不切回“在看”；最近列表合并两种状态并按最近活动排序，不能沿用只适用于电影的旧规则；
- 不设置数字或星级评分，继续使用包子本人的必填简短感受与人工推荐表达判断；
- 豆瓣评分、他人短评与平台排名不导入；选填字段缺失时直接省略，不生成假资料或占位内容。

V2 生产首发还要求：

- Blog、Photos、Project、Drinks、Books、Music、Movies 分别至少有一条满足上述要求的内容；
- About 使用用户批准的正式页面内容；
- 任何占位条目、假内容或仅用于布局测试的样本都不能计入栏目数量。

当前 schema 对 `description` 使用空字符串默认值，只能保证构建通过，不能代表内容达到发布要求。

现有全部公开详情页的用户批准状态记录在 `docs/project/content-approval-register.md`。构建成功只证明技术可生成，不证明内容可以生产发布。

## 6.3 可能的 schema 缺口

以下栏目专用需求只记录，不在 Phase 1 修改代码：

- 作品年份或完成状态；
- Photos 的相册字段；
- Project 与 Drinks 的特定字段；
- Project 的类型、时间区间、状态、角色，以及可选的背景、过程、成果 / 收获、图片与外部链接；
- Drinks 的名称、类型、饮用日期、地点、简短感受、可选“愿意再喝”，以及可选的品牌 / 酒厂、产地、酒精度、年份、照片、价格、搭配与外部链接；
- Books、Music、Movies 共用的书影音记录字段；
- Books 的作者、载体、自由文本状态、`currentlyReading`、记录 / 开始 / 完成日期，以及可选原名、译者、出版社、出版年份、封面、ISBN 与来源链接；
- Music 的记录类型（专辑 / EP / 歌单）、艺人 / 创建者、记录日期及音乐专用字段；
- Music 可选的原名、发行年份、流派、厂牌、封面、最喜欢的曲目与平台链接；
- Movies 使用统一主记录模型和必填 `kind: movie | series`；剧集核心字段为 `seasonCount` / `episodeCount`；每次完整看完写入作品级事件，重看新增事件，不建立逐季 / 逐集子记录；热力图只统计首次完成，累计使用互斥的“看过 / 在看”，最近列表合并两种状态并按最近活动排序；归档使用单一 `/movies/archive`，支持类型、状态、年份和标签筛选，每个作品只出现一次；
- 书影音热力图事件日期，以及推荐顺序、重点状态和“为什么推荐”；
- Photos、Project、Drinks、Books、Music、Movies 记录与相关 Blog 正文的关联字段；
- 豆瓣个人信息导出到本地内容的可审核导入映射。

`approved`、`updated`、`coverAlt`、`coverCredit` 是全站通用治理字段，不受“多条内容重复需要”条件限制。其他栏目专用字段只有在同一栏目至少多条内容反复需要时才扩展 schema，避免为单篇特例增加永久复杂度。

跨栏目内容采用单一正文来源：完整文章的规范 URL 位于 `/blog/<slug>`；主题栏目只保存自身所需的结构化字段、摘要与关联链接，不复制完整文章正文。

## 6.4 首页编辑规则

主页首要内容与日期规则如下：

- `date` = 内容原始发布日期或记录日期；
- `updated` = 可选的实质更新日期，不因修正错别字自动变化；
- 档案更新时间 = 全站公开内容中最大的 `updated ?? date`，不使用 Git mtime；
- 最近更新 = 从全部七个内容栏目中取 3 条公开且已批准内容，按 `updated ?? date` 倒序，并排除当前首要精选；
- 最近更新每条显示栏目、日期、标题和一句摘要；不足 3 条显示实际数量，没有可用内容时隐藏区块；
- 栏目归档 = 按 `date` 倒序，不使用手工编辑顺序；
- 首页首要内容优先使用全站唯一一条 `featured: true` 且公开、已批准的内容；
- 精选可以来自任意公开栏目；
- 若没有有效精选，只从 Blog、Photos、Project 三个主栏目回退到 `updated ?? date` 最新的公开且已批准内容；
- Drinks、Books、Music、Movies 可出现在最近更新，但不自动成为首要内容；
- 同时出现多于一条有效精选时阻塞发布，不按日期或 slug 自动决定；
- 草稿和未批准内容永远不参与更新时间、最近更新或精选回退；
- 零条有效内容的栏目不参与精选、最近更新或全站更新时间；其“正在整理”状态和 `noindex` 由有效内容数量自动派生。

## 6.5 占位内容

当前有多个公开占位条目。上线前必须：

- 设置 `draft: true`、Retire，或在用户确认后删除；
- 若复用同一文件路径，必须用用户提供的真实内容完整替换，并重新逐条批准，不能仅润色原占位文案；
- 任何占位条目都不能出现在栏目列表、详情、精选、最近更新或全站更新时间中，也不能计入七栏首发门禁；
- 不生成假文章、假记录或算法填充内容来补足栏目数量；
- 不得仅通过 `status: draft-concept` 假设它不会公开。

---

# 7. 交互、响应式与降级要求

## 7.1 卡片抽取

- 受控抽取，不做自由物理拖拽；
- 同时只展开一张；
- 堆叠态保留可读标题条；
- 展开态能查看完整图案和文字；
- 鼠标、触摸和键盘都能完成；
- 焦点不会进入被遮挡或 `inert` 的主页；
- 收回和打开手账是两个不同动作。

## 7.2 打开活页本

- 有明确按钮；
- 不要求先抽卡；
- 动画只是状态连续，不是加载器；
- `prefers-reduced-motion` 下立即完成；
- JavaScript 失败时主页内容仍在 HTML 中可读。

## 7.3 页面翻页

- 栏目入口仍是真实链接；
- 桌面可使用纸页方向的过渡；
- 手机可以改为短淡入/横移；
- 刷新和浏览器历史不依赖动画状态；
- 不建立完整 3D book engine。

## 7.4 响应式

### 桌面

- 桌面场景与活页本保持主要视觉关系；
- 抽出的卡片不超出可见阅读区；
- 打开按钮无需滚动即可发现。

### 手机

- 不把桌面整景等比缩成缩略图；
- 活页本和卡片使用独立单列或卡片轨道；
- 主页使用同一 DOM 阅读顺序的纵向周记；
- 触达区域至少 44 × 44px；
- 纵向滚动不被抽卡手势劫持。

---

# 8. 技术与维护约束

## 8.1 保留

- Astro 5 静态输出；
- Content Collections；
- Markdown / MDX；
- TypeScript；
- 原生 CSS / JavaScript；
- 本地字体和资源优化；
- Git 版本管理；
- Cloudflare Pages 目标部署；
- Obsidian 内容维护路径。

## 8.2 渐进增强

核心 HTML 应先包含：

- 主页标题和说明；
- 首要内容；
- 栏目索引；
- 最近更新；
- About 摘要。

JavaScript只负责：

- 启动状态显示；
- 卡片抽取；
- 状态焦点管理；
- 开本离场；
- 轻量页面过渡增强。

## 8.3 发布完整性

- `draft: true` 不出现在列表或详情；
- `approved: false` 不出现在生产列表或详情；
- 发布脚本在构建前核对所有输出内容满足 `draft: false && approved: true`；
- 构建失败不得发布；
- 图片和字体优化在生产构建前完成；
- Cloudflare Preview 用于开发与 QA；
- 自定义域名只指向通过 QA 的版本；
- 保留上一生产版本的 Git 回滚点。

---

# 9. 无障碍与 AI for UI 质量门槛

质量审查顺序：

1. 交互正确性；
2. 无障碍底线；
3. 布局稳定；
4. 视觉系统；
5. 动效；
6. 性能。

## Requirement

- 鼠标能完成的核心操作，键盘也能完成；
- 焦点可见，启动层关闭后焦点进入主页；
- 被启动层遮挡的主页不可被误聚焦；
- 栏目入口使用语义链接；
- 纯图标按钮有可读名称；
- 有意义图片有 alt；
- 不使用 `transition: all`；
- 不动画会引发布局抖动的核心尺寸属性；
- 减少动态效果时不等待抽卡、开本或翻页；
- 真实前景和纸张背景通过对比检查；
- 正文行宽、行高和标题换行可阅读；
- 不因局部效果引入大型运行时库。

审查问题使用：

- **Blocking：** 不修不能进入下一阶段；
- **Should fix：** 当前阶段应修；
- **Nits：** 最终打磨。

---

# 10. 交付优先级（Priority，不是项目 Phase）

## Priority P0 · V2 可用

- 启动页说明身份和用途；
- 有明确打开动作；
- 主页显示首要内容、全部七个内容栏目和最近更新；
- 七个内容栏目各至少有一条真实、公开且已批准内容；独立 About 和公开详情 URL 正常；旧 URL 永久跳转通过；
- 草稿不公开；
- 桌面、390px 手机和键盘可用；
- 减少动态效果不阻塞；
- check/build 通过。

## Priority P1 · 内容可信

- 所有首屏文案由用户批准；
- 不公开占位条目；
- About 和栏目说明完整；
- 关键页面使用真实内容与图片；
- 图片 alt 和来源完整；
- 页面层级在启动、主页、栏目和详情之间一致。

## Priority P2 · 视觉完整

- 真实桌面 / 透明活页本素材；
- 经过批准的卡片 Style Recipe；
- 栏目媒介差异有系统化规范；
- 页面翻页和微交互经过多端 QA；
- Cloudflare Preview 可供评审。

## Priority P3 · 发布完整

- SEO、OG、favicon、404；
- 自定义域名；
- 回滚方案；
- 可选匿名统计或 RSS；
- 维护和素材生成文档。

---

# 11. 验收剧本

## A · 第一次访问

在 1440×900 打开 `/`：

1. 不抽卡也能识别网站名、身份线索、用途和主动作；
2. 用鼠标和键盘各抽取一张卡；
3. 同一时间只有一张完整展开；
4. Escape 可收回；
5. 点击主动作进入主页；
6. 主页焦点落在合理的首要内容或标题。

## B · 手机访问

在 390×844：

1. 文字无需缩放即可阅读；
2. 可抽卡、收回和打开主页；
3. 页面能纵向滚动；
4. 不因卡片手势误阻塞滚动；
5. Blog、Photos、Project、Drinks、Books、Music、Movies 均可通过主页目录触达。

## C · 直接内容访问

直接打开任意 `/<section>/<slug>`：

- 不依赖先经过启动页；
- 能阅读正文；
- 能返回所属栏目和主页；
- 浏览器刷新、后退和新标签正常。

## D · 无 JavaScript和减少动态效果

- 无 JavaScript 时主页核心内容和栏目链接可读；
- `prefers-reduced-motion` 时不等待长动画；
- 启动层不会永久遮挡内容；
- 键盘路径不丢失焦点。

## E · 内容安全

- `draft: true` 不在列表；
- `approved: false` 不在生产列表；
- 草稿详情不生成；
- `status: draft-concept` 不承担公开门禁；
- 现有占位条目、假内容和未获批准内容不得进入生产发布或计入栏目数量；
- 主栏目与次栏目都必须满足至少一条 `draft: false && approved: true` 的真实内容；
- 预览中的零内容栏目显示“正在整理”并设置 `noindex`，且不进入精选、最近更新或全站更新时间；
- 第一条有效内容发布后，“正在整理”和 `noindex` 根据内容数量自动取消。

## F · 构建与发布

- `bun run check` 无 error；
- `bun run build` 成功；
- Cloudflare Preview 可访问全部主要路由；
- 生产部署校验确认七个内容栏目 `publishableCount >= 1`；任一栏目为零即阻塞部署；
- 生产发布前有可回滚版本。

---

# 12. 阶段与放行标准

## Phase 0 · Baseline

**完成。**

- 仓库、构建、内容、素材和文档已盘点；
- 当前未提交代码已记录；
- binder-archive 已确认为视觉来源；
- V1 文档冲突已识别。

## Phase 1 · Product definition

**当前进行中。**

交付：

- 本 PRD；
- 路由与页面层级；
- 每页信息需求；
- 用户内容输入表；
- 决策记录；
- Open Questions。

### Phase 1 关闭条件

1. `phase-1-content-input.md` 第一轮 1–18 的结构决策已记录；
2. 七个内容栏目与 About 的名称、说明、边界、目标路由、迁移、首发门禁和页面信息确认；
3. 启动页出现频率、两张信息卡正文、方案 B 装饰图片卡方向与素材清单确认；
4. 主页首要内容与最近更新规则确认；
5. 七个栏目各有一份可用于设计压力测试的真实内容样本计划，且所有现有占位内容的处理方向已记录；
6. 用户批准本 PRD 的页面结构；
7. 未解决项明确推迟到后续阶段，而不是默认为原型实现。

## Phase 2 · Design system & image recipes

Phase 1 关闭后开始：

- 视觉令牌；
- 页面栅格；
- 组件和状态；
- 动效规范；
- 图片 Style Recipes；
- 真实素材槽位和 Prompt 包。

## Phase 3–7

依次为交互风险原型、高保真设计确认、正式开发、内容与 QA、Cloudflare 发布。阶段任务会在 Phase 1 通过后写入 Linear。

现有 `binder-archive` 已完成“方向是否可行”的技术试验，不需要从零重做。Phase 3 将以它为基础，替换 Phase 1 已确认的真实内容和 Phase 2 规范，重点复测卡片数量、回访策略、真实素材、移动端与页面翻转，而不是再次探索其他视觉方向。

---

# 13. Phase 1 Open Questions

所有答案由用户提供，填写位置为 `docs/project/phase-1-content-input.md`。

已关闭：

- **OQ-01：** 网站主名称确认为“中华一番包子铺”，个人公开署名确认为“包子”。
- **OQ-02：** 一句话身份、Work / Life 短介绍、网站用途和基本隐私边界已确认。
- **OQ-03：** 每个浏览会话显示一次 Launch；关闭标签页后失效；主页可“合上本子”；从内页返回 `/` 直接进入 Home。
- **OQ-04：** Launch 只使用网站简介与 About Me 两张信息卡；栏目目录、最近更新和全站更新时间全部留在 Home。
- **OQ-05：** 网站简介卡只阅读；About Me 卡使用真实链接进入 `/about`。
- **OQ-06：** 主页优先展示全站唯一人工精选；没有精选时回退到 Blog、Photos、Project 的最新公开且已批准内容。
- **OQ-07：** 最近更新固定 3 条，覆盖全部七个内容栏目，按 `updated ?? date` 倒序并排除当前首要精选。
- **OQ-08：** `/projects`、`/movies` 及七内容栏目 + 独立 About 的目标路由已经确认。
- **OQ-09（迁移部分）：** Thoughts 并入 Blog、AI Works 并入 Project；旧详情 URL 逐条永久跳转。
- **OQ-09（Blog 部分）：** Blog 是包子的文章集合，包含长文与短文；完整文章统一归 Blog，相关主题栏目保存结构化记录并关联正文，不重复维护全文。
- **OQ-09（Photos 结构部分）：** Photos 以相册 / 摄影系列为发布单位，每条记录包含多张照片、时间、地点和简短说明；V2 不建立独立单张照片流。
- **OQ-09（Photos 说明部分）：** 相册简介必填，逐图 caption 可选；有意义的照片仍必须提供 alt。
- **OQ-09（Photos 素材部分）：** 只收录包子亲自拍摄的数码、胶片及底片扫描；AI 图片、设计拼贴与第三方摄影作品不进入 Photos。
- **OQ-09（Photos 隐私部分）：** 家人照片永不公开；包子本人出镜逐条批准，其他可识别人物只有获得明确同意后才能发布。
- **OQ-09（Photos 文案部分）：** 正式栏目说明为“用相册和摄影系列，记录我看见并想留下的生活。”
- **OQ-09（Project 范围部分）：** Project 同时收录可公开的职业项目与个人项目，包括产品、营销实践、网站、AI 创作和实验；完整叙事仍归 Blog。
- **OQ-09（Project 保密部分）：** 职业项目只使用公开可验证信息及包子自己的职责、方法和反思；内部数据、客户信息、内部截图和文件不公开。
- **OQ-09（Project 结构部分）：** 标题、类型、时间、状态、角色和一句话简介必填；背景、过程、成果 / 收获、图片、外部链接及相关文章按项目选填。
- **OQ-09（Project 状态部分）：** 状态使用必填自由文本，不设固定词表，也不参与发布门禁或其他自动逻辑。
- **OQ-09（Project 文案部分）：** 正式栏目说明为“记录我参与、完成和持续试验的项目。”
- **OQ-09（Drinks 结构部分）：** 一款酒、一瓶酒或一杯调酒对应一条记录，饮用时间与地点属于记录；一次多酒款聚会拆成多条，完整经历归 Blog。
- **OQ-09（Drinks 字段部分）：** 名称、类型、饮用日期、地点和简短感受必填；品牌 / 酒厂、产地、酒精度、年份、照片、价格、搭配、外部链接及相关文章选填。
- **OQ-09（Drinks 去重部分）：** 同一款酒只保留第一次饮用记录，之后再次饮用不新增条目；栏目数量表示记录过的唯一酒款数。
- **OQ-09（Drinks 评价部分）：** 不设数字或星级评分；使用必填的简短感受与可选“愿意再喝”标记，且不生成自动排行。
- **OQ-09（Drinks 文案部分）：** 正式栏目说明为“关于酒款、地点与味道的个人记录。”
- **OQ-09（Books 范围部分）：** Books 只收录纸质书、电子书与有声书，每本书一条；长文章、论文、杂志、Newsletter 与网络文章不进入 Books。
- **OQ-09（Books 状态部分）：** 对外阅读状态使用必填自由文本，不设固定词表；“正在读 Top 3”必须使用独立机器规则，不能从状态文案猜测。
- **OQ-09（Books 当前阅读部分）：** 使用 `currentlyReading` 布尔标记；超过 3 本时按 `updated ?? date` 取最近 3 本，少于 3 本显示实际数量，零本时隐藏区块。
- **OQ-09（Books 字段部分）：** 书名、作者、载体、自由状态、记录日期和简短感受必填；扩展书目信息、阅读日期、封面、链接及相关文章选填。
- **OQ-09（Books 评价部分）：** 不设数字或星级评分；使用简短感受和人工推荐区表达判断，不生成评分排行。
- **OQ-09（Books 文案部分）：** 正式栏目说明为“记录读过和正在读的书，以及它们留下的想法。”
- **OQ-09（Music 范围部分）：** Music 只收录主动记录的专辑、EP 与歌单；单曲、播放事件、播放次数和流媒体自动历史不进入 Music。
- **OQ-09（Music 最近部分）：** “最近在听”按 `updated ?? date` 从公开已批准记录自动取最多 5 条；不足显示实际数量，零条隐藏，不设人工开关或排名。
- **OQ-09（Music 字段部分）：** 名称、类型、艺人 / 创建者、记录日期和简短感受必填；扩展音乐资料、封面、最喜欢的曲目、链接及相关文章选填。
- **OQ-09（Music 评价部分）：** 不设数字或星级评分；使用简短感受和人工推荐区表达判断，不生成评分排行。
- **OQ-09（Music 文案部分）：** 正式栏目说明为“记录最近在听，以及想长期留下的音乐。”
- **OQ-09（Movies 范围与模型部分，已关闭）：** D-092 将范围扩展为电影与剧集，综艺和网络视频继续排除；D-093 确认对外名称为 `Movies`、路由保留 `/movies`；D-094 确认正式说明；D-095 确认电影与剧集共用统一主记录并以必填 `kind: movie | series` 区分。
- **OQ-09（Movies 最近与归档部分，已关闭）：** 作品级观看事件与重看事件已确认；热力图只统计首次完成；累计统计使用互斥的“看过 / 在看”；最近列表合并两种状态，每个作品只出现一次，按进入“在看”的时间或最近完成事件统一倒序，Top 5，手机端前 3 条加后 2 条横向访问；单一 `/movies/archive` 支持类型、状态、年份和标签筛选，重看历史只在详情页呈现。
- **OQ-09（Movies 字段部分，部分关闭）：** 主记录模型与类型判别已确认；剧集核心字段 `seasonCount` / `episodeCount` 已确认；共同字段与事件日期口径待确认；累计统计使用互斥的“看过 / 在看”，首次完成后转为“看过”，重看不切回“在看”；单集时长、平台、主演等保持选填，且不建立逐季 / 逐集子记录。
- **OQ-09（Movies 评价部分）：** 不设数字或星级评分；使用简短感受和人工推荐区表达判断，不生成评分排行。
- **OQ-09（Movies 文案部分，已关闭）：** 正式说明确认为“记录看过的电影与剧集，以及它们留下的感受。”
- **OQ-09（部分重开）：** Blog、Photos、Project、Drinks、Books、Music 规则保持已确认；Movies 因 D-092 扩展至剧集，名称、文案、字段、热力图与最近规则重新打开。
- **OQ-12：** `/about` 直接作为完整 About 页面，旧 `/about/me` 永久跳转。
- **OQ-15：** Books、Movies、Music 使用独立栏目主页和独立完整归档页；栏目主页为热力图 → 最近状态 → 全部记录入口 → 人工推荐。
- **OQ-17：** 启动页采用方案 B，保留装饰图片卡；Demo 以 3 张为基线，Phase 2 可在 3–5 张内确定最终数量。装饰图片只展示图像，不承担栏目导航或动态信息。
- **OQ-18：** V2 生产首发不允许空栏目；七个内容栏目使用同一发布门禁。异常空状态保留 URL、显示“正在整理”、设置 `noindex`，并在第一条有效内容发布后自动恢复。

| ID | 问题 | Owner | 最迟决策阶段 |
|---|---|---|---|
| OQ-10 | 已确认真实姓名与家人信息不公开；About 还愿意公开哪些个人事实和联系方式？ | 包子 | Phase 4 |
| OQ-11 | favicon / 品牌标志采用什么素材？ | 包子 | Phase 2 |
| OQ-13 | 全站 title 与 meta description 是什么？ | 包子 | Phase 4 |
| OQ-14 | OG 图、图片署名规则与匿名统计是否启用？ | 包子 | Phase 6 |
| OQ-16 | 豆瓣个人信息导出是否包含所需书影音记录；采用本地审核导入还是仅保留外链？ | 包子 + Codex | Phase 2 |
